前一步是把数据长什么样先定下来，这一步是把这些正式数据真的放到云端保存好。

您不用重新想一遍，也不用重新设计一套。只需要让 Codex 先看看项目里已经写过的数据说明、需求记录和项目说明，再按现有内容把这些表建好。

从这一步开始，项目里的正式业务数据都走云端。不要再把本地假数据、本地数据库文件、临时保存文件这类东西，当成正式数据来源。

操作方式也尽量简单：在本地打开 Codex，模型选 `GPT-5.4`。如果您愿意，也可以先让它先列一个简短计划，再开始动手。把下面这段话直接复制进去即可。

```markdown
在本地打开 Codex，模型使用 GPT-5.4。

请帮我把这个项目已经确定好的正式数据放到云端数据库里。开始前，先看看仓库里现有的数据说明、项目记录和相关文档，不要脱离现有内容重新想一套。

要求：
1.先沿用项目里已经定好的数据结构，缺什么补什么，不要重起炉灶。
2.帮我把云端数据库准备好，并把这些表真正建进去。这里默认用 Cloudflare D1。
3.从这一步开始，这个项目的正式数据以后都只放在云端。
4.不要再把本地假数据文件、本地数据库文件、临时保存文件当成正式数据来源。
5.如果仓库里已经有这类本地数据方案，请一起找出来，并改成以云端数据库为准。
6.所有回复必须用中文。
7.不要输出代码 diff。
8.如果适合，可以先给我一个简短计划，再开始执行。

输出时请告诉我：
1.你帮我准备了什么
2.现在正式数据放在哪里
3.哪些本地数据方式以后不能再继续用
4.我应该去哪里验收
```