请根据我的功能文档 `features.md` ，帮我设计一版“够实现业务就行”的数据库表结构`.sql`文档，要求如下：

1.不要过度设计，优先最小可用方案
2.字段尽量简单，优先使用标准 SQL 常见字段
3.以 Cloudflare D1 可执行的 SQL 为准，按 SQLite 语法输出
4.尽量少用复杂 JSON、枚举、触发器、视图、存储过程
5.字段命名尽量直白，优先参考使用：
   -`id`
   -`user_id`
   -`status`
   -`created_at`
   -`updated_at`
6.请先按“能把业务做出来”来设计，不要一开始就加很多以后也许才会用到的高级字段
7.如果我的需求没写清楚，请优先给最简单、最稳妥的方案，不要自行扩成很复杂的系统

请按以下格式输出：

1.先用人话告诉我，一共需要哪几张表，每张表是干什么的
2.再输出完整的建表 SQL
3.再输出必要的索引 SQL
4.再输出每张表的字段说明
5.再输出这些表之间是什么关系

如果需要举例帮助理解，可以参考下面这套最容易理解的“用户-订单”示例，但最终表结构必须以我的 `features.md` 真实业务为准：
-`users`：用户
-`products`：商品
-`orders`：订单
-`order_items`：订单明细
-`payments`：支付记录

请注意：
-不要写 MySQL / PostgreSQL 专属语法
-主键、外键、时间字段尽量统一风格
-输出内容要让完全不懂技术的人也能大致看懂