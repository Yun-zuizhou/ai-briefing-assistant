请先帮我完成这个项目当前阶段的“环境初始化与启动收口”。

目标不是给我讲技术原理，而是基于我这个项目当前真实代码结构、当前文档主线和当前阶段任务，把运行、联调、云端数据源和启动方式尽量收口，让我最后只需要执行尽可能少的命令，就能把项目跑起来并进入当前阶段工作。

请你严格按下面要求工作：

1. 开始前先按我当前项目文档工作流建立正确上下文
- 先阅读 `文档/文档规范.md`
- 再阅读 `文档/项目核心总纲.md`
- 再优先阅读 `文档/进行中/` 下与当前阶段直接相关的文档
- `文档/项目功能文档.md` 只作为“当前代码里已存在功能和页面现状”的参考，不能覆盖当前阶段判断
- 如果旧文档、README、旧功能描述与当前阶段文档冲突，以当前阶段文档为准

2. 先自动判断项目现在到底是如何运行的
- 检查真实代码结构、目录分工和当前启动方式
- 判断用的是什么包管理方式
- 页面怎么启动
- 业务逻辑怎么启动
- 是否已有统一启动脚本
- 需要什么版本的 Node
- 是否依赖 Python、wrangler 或其他运行环境
- 是否依赖数据库
- 是否依赖 Cloudflare 相关能力
- 当前正式数据源应该指向哪里
- 哪些链路仍然是 mock、虚拟、回退或过渡态
- 不允许把过渡态误判为正式态

3. 先确认“当前正式态”和“当前过渡态”，再开始收口
- 正式业务数据必须优先走云端数据库
- 不要新增新的本地正式持久化方案
- 如果当前仍存在本地模拟、共享虚拟状态、接口回退或过渡逻辑，请明确区分：
  - 哪些是正式态
  - 哪些是虚拟态
  - 哪些是回退态
  - 哪些只是为了联调存在的临时方案
- 如果某条链路现在只能本地模拟，必须明确告诉我，不要让我误以为已经真正上云

4. 请把项目运行所需内容尽量补齐
- 缺少依赖安装脚本就补
- 缺少统一启动脚本就补
- 缺少前后端联调所需的最小启动方案就补
- 缺少云端数据库初始化、建表、准备最小测试数据的脚本就补
- 缺少 `.env.example` 就补
- 缺少联调用的最小测试用户或最小可运行数据，就补一套最小方案
- 但不要新引入本地正式数据库、本地正式假数据文件或其他新的本地正式持久化方案

5. 所有复杂命令尽量收口到项目现有可维护位置
- 优先考虑 `package.json` 的 scripts
- 如果项目不是纯 Node 结构，也可以结合现有 `scripts/`、Python、wrangler 或其他方式做统一收口
- 我的目标不是死守某一种形式，而是让我最后尽可能少记命令
- 最理想目标是只保留一条启动命令，例如 `npm run dev`
- 如果受项目真实结构限制，确实无法压缩到一条命令，请压缩到最少步骤，并明确为什么无法继续收口

6. 请优先修复会阻塞当前阶段工作的启动问题
- Node 版本不兼容
- Python 或其他运行环境缺失
- 依赖没安装
- 端口冲突
- 环境变量缺失
- 云端数据库未初始化
- 建表或最小测试数据未准备
- 启动脚本缺失
- 前端和后端连不起来
- 本地模拟与云端正式库混淆
- 当前阶段主线要求与当前启动方式不匹配
- 但不要主动把这次任务扩展成新的页面重构、产品结构重写或信息架构改造

7. 遇到需要权限、密钥、云端账号或网络限制时，立即收敛
- 能自动修的就直接修
- 不能自动修的，再告诉我我只需要执行哪一条命令，或只需要补哪一步配置
- 如果需要我补信息，只问最少的问题
- 不要先给我很多解释，再让我自己判断该怎么做

8. 请遵守我当前项目的文档工作流
- 项目总锚点文档是 `文档/项目核心总纲.md`
- 当前阶段记录优先写入 `文档/进行中/`
- 新增阶段记录文件必须保留时间戳命名，例如 `2026-04-02-环境初始化与启动收口.md`
- 阶段未真正结束前，不要擅自归档到 `文档/归档/`
- 如果这次环境初始化、启动收口、云端数据库落地属于当前阶段的重要推进，请把记录写进 `文档/进行中/`
- 归档时不允许清洗掉时间戳

9. 不要给我讲太多技术原理，只给我结果，但要保留结构化技术日志
- 不要输出代码 diff
- 不要输出大段 CLI 日志
- 但你要给我一段“结构化技术日志”，至少说明：
  - 这次触及了前端 / 后端 / 数据 / 云端 / 文档中的哪些层
  - 当前正式数据源在哪里
  - 哪些仍然是 mock、虚拟、回退或过渡方案
  - 这次做了哪些关键技术选择
  - 当前项目启动方式是否已经真正收口
  - 如果还没完全收口，卡点是什么

10. 所有回复必须用中文

最后请按下面格式给我结果：

1. 我已经帮您完成的环境初始化：
2. 请您现在执行的启动命令：
3. 如果因权限、云端配置或环境限制还不能直接启动，我还需要您补的唯一一步：
4. 当前阶段记录已写入：
5. 结构化技术日志：
6. 说明：
