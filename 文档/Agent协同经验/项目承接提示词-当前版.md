# 项目承接提示词-当前版

- 文档定位：新对话快速接手状态卡
- 使用方式：本文件只维护"当前项目现状 + 当前下一步"，默认配合 `文档/Agent协同经验/项目初始化.md` 一起使用
- 更新时间：2026-04-30（P4 正式页面迁移启动，P4-A0 执行中）

```md
# 当前项目状态注入卡

你现在接手的项目是：

- 项目名：AI简报助手（时代与我）
- 当前日期：2026-04-30
- 当前阶段名称：P4 正式页面迁移（复古报刊简报组件迁入正式页面）
- 当前执行主线：**P4-A0 对话摘要契约适配 + 负资产清理**
- 当前最近一轮已完成：
  1. `/decor-preview` S5 QA 通过，可迁移组件已明确（EditorialMasthead / EditorialIcon / PaperButton / StatusBadge）
  2. P4 正式页面迁移计划已建立，分 P4-A0 → P4-A~E 六批执行
  3. 负资产分类已明确：`apps/web/demo/`（8.1MB 早期副本）、`PreviewPage.tsx`（旧预览索引）、Bookish 系列（暂缓迁移）

使用边界：

1. 本文件只做当前状态注入
2. 本文件不长期承接阶段过程流水
3. 更细的阶段事实回到 `当前阶段总表.md` 与主线文档
4. 本卡用于"快速接手"，不替代核心文档和当前阶段文档的执行判定职责

## 一、先按这个顺序建立上下文

1. `文档/项目核心总纲.md`
2. `文档/项目核心子纲-阶段演进与归档索引.md`
3. `文档/技术现实清单.md`
4. `文档/README.md`
5. `文档/进行中/当前阶段总表.md`
6. `文档/进行中/当前测试与验收总表.md`
7. `文档/进行中/2026-04-30-P4正式页面迁移计划.md`
8. `文档/进行中/2026-04-30-P4-A0-对话摘要契约适配与demo清理.md`
9. `文档/进行中/2026-04-29-预览组件一致性整理-S5QA验收与迁移门禁.md`
10. 真实代码、脚本、配置、测试

当前代码目录口径补充：

1. 前端：`apps/web/src/`
2. 正式在线后端：`apps/edge-worker`
3. Python 离线/参考/验证后端：`app`
4. 共享契约：`packages/contracts`
5. D1 迁移与 seed：`infra/cloudflare/d1`
6. 工具脚本：`tools/scripts`
7. 负资产（待删除）：`apps/web/demo/`（早期探索副本，不被正式构建引用）

## 二、当前最重要的项目判断

当前项目已经进入**正式页面视觉迁移阶段**，不再处在"有没有接口"或"稳不稳定"的阶段：

1. P4-A0 先补契约适配层，再删 demo 负资产
2. 后续 P4-A~E 按顺序把复古报刊组件迁入对话/待办/成长/我的/Today
3. 不跨批次顺手重构，不把样张整块搬进正式页
4. P4-D（我的页删入口）必须等 A+C 补完入口后再执行

因此当前不要回到：

1. 收益排序
2. 对象扩面
3. 旧接口拼装
4. mock 主驱动页面
5. Bookish 组件扩大使用面

## 三、当前测试与运行口径

1. 唯一准入门槛：`npm.cmd run check`
2. 当前测试数字与最近一次复验结果，以 `文档/进行中/当前测试与验收总表.md` 为准
3. Windows 标准入口只保留：
   - `npm.cmd run setup`
   - `npm.cmd run dev`
   - `npm.cmd run check`
4. 高频事项优先走 `文档/命令规范/常用一键命令.md` 与 `task:*` 快捷入口

## 四、当前不要误判的点

1. 不要把 `apps/web/demo/` 当成正式前端代码
2. 不要把 demo 数据误写成正式事实源
3. 不要把 Bookish 组件扩大进正式页面
4. 不要把样张组件（EditorialChatSample 等）整块复制到正式页
5. P4 不改后端 API、数据库 schema 和共享类型契约
6. P4 不删旧路由，只降级入口

## 五、当前下一步建议

### P4-A0（当前执行中）

1. 补 `apiService` 系统摘要任务方法（4 个方法）
2. 调整 `ChatRecordsPanel` 组件契约（摘要/会话双 tab）
3. 删除 `apps/web/demo/`（8.1MB）
4. 降级 `/preview` 路由

### P4-A~E（后续顺序执行）

5. P4-A：对话页记录子页迁移
6. P4-B：待办页行动工作台迁移
7. P4-C：成长页画像与 AI 分析迁移
8. P4-D：我的页设置中心迁移（等 A+C 入口补完）
9. P4-E：Today 与报告页回补

## 六、一句话接手结论

P4 正式页面迁移已启动。当前在 P4-A0：先补 ChatRecordsPanel 的摘要契约适配层（复用已有 `/api/v1/system/summary-tasks` 链路），同步删除 `apps/web/demo/` 负资产。完成后进入 P4-A 对话页视觉迁移。不跨批次重构，不扩大 Bookish 使用面。
```
