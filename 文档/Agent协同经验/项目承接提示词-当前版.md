# 项目承接提示词-当前版

- 文档定位：新对话快速接手状态卡
- 使用方式：本文件只维护"当前项目现状 + 当前下一步"，默认配合 `文档/Agent协同经验/项目初始化.md` 一起使用
- 更新时间：2026-04-22（命令规范与文档功能区分层已同步）

```md
# 当前项目状态注入卡

你现在接手的项目是：

- 项目名：AI简报助手（时代与我）
- 当前日期：2026-04-19
- 当前阶段名称：阶段十五完成后的产品尾差清理与仓库结构收口
- 当前执行主线：`保持 check 全绿，同时继续清页面级产品尾差`
- 新增主线：`运行层目录归位与接口契约收口（已落地）`
- 当前最近一轮已完成：
  1. 历史报告列表支持真实 `report_id`
  2. `HistoryBriefPage` 可按 `report_id` 打开具体历史周报 / 月报 / 年报
  3. `Actions` 打卡态已并入后端真实状态，支持 `POST /actions/check-in`
  4. `Growth keywords` 已从系统噪音词收成用户画像关键词
  5. 内容详情在正文缺失时已补最小可读正文
  6. `content_processing / content_projection / content.py` 可单独导入测试
  7. Pydantic / SQLAlchemy deprecation warnings 已清零
  8. 前端大包告警已清掉，路由页已懒加载
  9. 当前唯一门槛 `npm.cmd run check` 已复验通过
  10. **新增**：运行层目录已收口到 `apps/web`、`apps/edge-worker`、`tools/scripts`、`infra/cloudflare/d1`
  11. **新增**：前后端共享页面契约已收口到 `packages/contracts/src/page-data.ts`
  12. **新增**：登录页已支持 `新用户新邮箱 / test@example.com / show@example.com` 三类测试邮箱快速切换
  13. **新增**：云端 D1 已完成测试身份重排，`show@example.com` 已形成独立展示数据
  14. **新增**：Pages 正式域名已刷新到 `apps/web` 最新 bundle，线上前端已与当前 session 鉴权和 Workers 路由口径对齐
  15. **新增**：前端目录已完成第一轮正式 / demo 分层，正式页面与演示页面已不再共用同一状态层，历史 compat 壳页已清理
  16. **新增**：已完成一轮系统归档整理，阶段十四旧主线已入归档，Railway/旧评估文档已降级到 `文档/过时`
  17. **新增**：已完成核心区文档与项目资产第二轮收口：
      - 当前项目设计归档已统一装箱到 `AI简报助手（时代与我）/`
      - `文档/学术答辩` 已按项目拆分
      - 当前主门槛再次复验通过，具体数字以 `当前测试与验收总表.md` 为准
  18. **新增**：项目管理流程已继续收口：
      - 已新增 `当前测试与验收总表.md`
      - 已新增“突破升级触发器”硬规则
  19. **新增**：`ArticlePage / HotTopicsPage` 最明显的一批研发态文案已完成产品化清理，并已通过前端构建验证
  20. **新增**：`ChatPage / CollectionsPage / HistoryLogsPage` 已继续清掉一批内部引用字段展示点，页面跳转和上下文能力保持不变
  21. **新增**：`ChatPage` 的识别元信息已改成自然语言，三类报告页的可信度展示已完成中文化
  22. **新增**：已新增“核心页面人工验收与截图证据清单”，`e2e/app-mainline.spec.ts` 已于 2026-04-19 再次复验通过
  23. **新增**：已建立 `task:*` 高频一键命令命名空间，常用运行 / 权限 / 预检事项已有稳定快捷入口
  24. **新增**：文档功能区已完成一轮物理清理，`文档/命令规范/` 已独立成专题目录，旧 UI / 产品 / 技术资料已分别转入归档或过时区

使用边界：

1. 本文件只做当前状态注入
2. 本文件不长期承接阶段过程流水
3. 更细的阶段事实回到 `当前阶段总表.md` 与主线文档
4. 本卡用于"快速接手"，不替代核心文档和当前阶段文档的执行判定职责

## 一、先按这个顺序建立上下文

1. `文档/项目核心总纲.md`
2. `文档/项目核心子纲-阶段演进与归档索引.md`
3. `文档/技术现实清单.md`
4. `文档/README.md`
5. `文档/进行中/当前阶段总表.md`
6. `文档/进行中/当前测试与验收总表.md`
7. `文档/进行中/2026-04-18-项目管理流程优化与测试事实源收口.md`
8. `文档/进行中/2026-04-16-正式主线收口标准与接口契约设计.md`
9. 真实代码、脚本、配置、测试

当前代码目录口径补充：

1. 前端：`apps/web`
2. 正式在线后端：`apps/edge-worker`
3. Python 离线/参考/验证后端：`app`
4. 共享契约：`packages/contracts`
5. D1 迁移与 seed：`infra/cloudflare/d1`
6. 工具脚本：`tools/scripts`
7. 本地运行产物：`var/local`、`var/logs`

## 二、当前最重要的项目判断

当前项目已经不再处在"主线有没有接口"的阶段，而是在：

1. 保持唯一正式仓库与唯一门槛的稳定状态
2. 清理页面层最影响体感的产品尾差
3. 继续做真实页面人工验收
4. **新增**：维持新仓库结构下的路径、脚本与契约稳定
5. **新增**：用 3 类测试邮箱补齐生产环境截图级证据和演示口径
6. **新增**：继续清理 `src/demo` 中历史资产的归档/删除边界

因此当前不要回到：

1. 收益排序
2. 对象扩面
3. 旧接口拼装
4. mock 主驱动页面

## 三、当前测试与运行口径

1. 唯一准入门槛：`npm.cmd run check`
2. 当前测试数字与最近一次复验结果，以 `文档/进行中/当前测试与验收总表.md` 为准
3. Windows 标准入口只保留：
   - `npm.cmd run setup`
   - `npm.cmd run dev`
   - `npm.cmd run check`
4. 高频事项优先走 `文档/命令规范/常用一键命令.md` 与 `task:*` 快捷入口
5. `npm.cmd run test:api-mainline` 只保留为辅助诊断

## 四、当前不要误判的点

1. 不要把"主线稳定"误写成"产品级全量完成"
2. 不要把 demo 数据误写成正式事实源
3. 不要把 `HistoryBrief` 再理解成"固定跳当前报告页"
4. 不要把 `Actions` 的打卡态再改回前端本地假状态
5. 不要把 `Growth keywords` 再退回系统噪音标签
6. **新增**：不要把根目录旧路径再当成正式代码入口
7. **新增**：根目录 `worker.js / wrangler.toml` 已归档，不再是正式 Cloudflare 入口
8. **新增**：Python `app/` 当前仍是离线/参考/验证后端，不等于正式在线 API 主线

## 五、当前下一步建议

### 产品层主线（当前进行中）

1. 继续做 `Today / Actions / Growth / My / Weekly` 的真实页面人工验收
2. 用 `新用户新邮箱 / test@example.com / show@example.com` 补齐生产环境截图级证据
3. 根据验收结果继续收页面级缺陷，而不是回到对象扩面
4. 按新清单执行一轮核心页面截图级人工验收，并记录“已截图 / 未截图 / 有问题”
5. 对正式页面继续排查并清理少量仍直接暴露内部字段的展示点
6. 对 demo 历史资产做最后一轮归档/删除判断，避免继续污染正式前端认知

### 结构收口主线（已落地）

1. 正式前端已迁入 `apps/web`
2. 正式在线后端已迁入 `apps/edge-worker`
3. D1 迁移与 seed 已迁入 `infra/cloudflare/d1`
4. 前后端共享页面契约已迁入 `packages/contracts`

### 阶段切换条件

当前阶段继续收口的条件：
1. 产品层尾差清理完成（ArticlePage 研发态文案已清理）
2. 核心页面人工验收完成（Today/Actions/Growth/My/Weekly）
3. 新目录结构下的 `check / check:workers / db:d1:prepare` 持续稳定通过

## 六、一句话接手结论

当前项目的工程稳定化已经基本完成，主线接口、历史报告、打卡态、关键词、正文兜底、测试口径都已站稳。现在最重要的工作是继续清用户可见的产品尾差，同时维持新目录结构、共享契约和 D1 目录口径不再回退。
```
