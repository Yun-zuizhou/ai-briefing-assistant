# 当前测试与验收总表

- 文档状态：进行中
- 更新日期：2026-04-27
- 文档定位：当前唯一测试数字事实源

---

## 1. 使用规则

1. 当前测试数字只允许写在本文件。
2. 其他核心文档不得重复维护具体测试数字。
3. 其他文档只允许写：
   - 已复验通过 / 未通过
   - 以《当前测试与验收总表》为准
4. 只有在真实执行对应入口后，才能更新这里的数字和状态。
5. 更新本文件前，优先执行 `npm.cmd run task:verify:proof` 并引用其摘要文件。

---

## 2. 当前主门槛快照

### 2.1 主门槛

- 命令：`npm.cmd run check`
- 最近复验日期：2026-04-27
- 结果：通过

### 2.2 当前结果

| 维度 | 当前结果 |
|------|---------|
| Python 主套件 | `76 passed` |
| Workers | `19 files / 105 tests` |

### 2.3 本轮执行摘要（2026-04-27）

1. `npm.cmd run check`：通过（含 `apps/web build + lint`、Python `76 passed`、Workers `19 files / 105 tests`）
2. `npm.cmd run task:verify:proof`：通过，自动生成摘要 `var/logs/gate-check/latest-summary.md`
3. `npx.cmd playwright test e2e/app-mainline.spec.ts`：通过（`6 passed`）
4. 本轮摘要文件：`var/logs/gate-check/20260427-213022-summary.md`
5. 当前 `check` 与 `task:verify:proof` 在受限沙箱内会出现 `spawn EPERM`，本轮已在可执行权限下复验通过并记录结果

---

## 3. 当前辅助门槛

| 命令 | 角色 | 当前状态 | 备注 |
|------|------|---------|------|
| `npm.cmd run check:workers` | Workers 专项门槛 | 已并入主门槛验证链 | 以最近 `check` 结果为准 |
| `npm.cmd run check:proof:strict` | 双命令证据门槛 | 已复验通过 | 同时留存 `check` 与 `check:workers` 摘要证据 |
| `npm.cmd run preflight:workers-sync` | D1 同步预检 | 参考口径 | 当前未在本轮重跑 |
| `npm.cmd run preflight:workers-config` | Workers 配置预检 | 已复验通过 | 本地 `.dev.vars` 与默认 Worker secret 已通过 |
| `npm.cmd run test:api-mainline` | 辅助诊断 | 非主门槛 | 不再单独代表稳定结论 |
| `npm.cmd run test:e2e:workers` | 浏览器 E2E 总入口 | 需要时补跑 | 当前未在本轮重跑 |
| `npx.cmd playwright test e2e/app-mainline.spec.ts` | 浏览器 E2E | 已复验通过 | 2026-04-27 阶段 2 收尾验收与 Today briefing 路由小补丁后重跑 `6 passed` |

---

## 4. 当前验收判断

### 4.1 自动化层

当前可以确认：

1. 主门槛 `check` 已在当前机器权限下复验通过。
2. Workers 类型检查与 Vitest 已通过。
3. 当前测试数字以本文件为唯一准。
4. 本轮已修复 `tests/test_dashboard_today.py` 的 3 处字符串断言漂移（原 `3 failed`），修复后主门槛恢复全绿。

### 4.2 手工验收层

当前仍需继续补：

1. `Today / Actions / Growth / My / Weekly` 的截图级人工验收
2. “关注 AI -> 推荐命中 AI”场景的人工证据
3. 正式鉴权链替换后的截图级新验收口径

### 4.3 浏览器自动化补充

当前可以确认：

1. `e2e/app-mainline.spec.ts` 已在本轮复验通过。
2. 未登录访问受保护路由被拦到 `/welcome`、登录后会话恢复、退出后再次拦截，均已具备浏览器级自动化证明。
3. `auth-guard.route.test.ts` 已在本轮复验通过，主要受保护 API 路由的未登录 `401` 已具备批量自动化证明。
4. 本轮浏览器复验使用临时 `5174` 前端端口与 `8788` Workers 后端端口执行，前端 API 走同源代理。
5. 本轮已补齐 Playwright Chromium 本地运行环境，浏览器级主文件复验不再卡在“缺浏览器可执行文件”。

---

## 5. 更新方式

当下一次更新测试结果时，按下面顺序执行：

1. 先执行 `npm.cmd run task:verify:proof`（必要时用 `npm.cmd run check:proof:strict` 做双命令证明）
2. 从 `var/logs/gate-check/latest-summary.md` 复制当轮摘要
3. 再更新本文件中的数字
4. 再去上层文档同步“已复验通过/未通过”的结果级状态

不允许反过来先改上层文档。
