# UI改进-批次6-体验层执行记录

- 文档定位：`2026-04-22-UI设计系统落地与页面层尾差清理.md`的子文档（线A-批次6）
- 建立日期：2026-04-22
- 批次目标：骨架屏 + 过渡动画 + 可访问性补齐
- 预计文件数：5-8
- 风险等级：低
- 复验方式：`npm.cmd run check` + E2E复验
- 关联问题清单：
  - 问题03-4（前端测试缺失）：同步引入Vitest + React Testing Library，为Skeleton、ErrorBoundary编写单元测试

---

## 1. 执行前状态

### 1.1 当前问题

| 问题 | 当前状态 | 目标状态 |
|------|---------|---------|
| 无骨架屏 | 全部页面loading态为文字"加载中..." | 各页面有对应的骨架屏 |
| 无过渡动画 | 路由切换生硬 | 路由切换有淡入淡出 |
| 可访问性不足 | 对比度2.7:1、缺少aria-label | 对比度≥4.5:1、所有交互元素有aria-label |
| 无焦点状态 | 键盘Tab导航不可见 | 全局focus-visible样式 |
| 无减少动画支持 | 未考虑prefers-reduced-motion | 添加媒体查询 |

### 1.2 依赖关系

- **前置依赖**：批次3/4/5（页面层）完成
- **后置影响**：批次7（架构层）可复用骨架屏组件

---

## 2. 文件修改清单

### 文件1：`apps/web/src/components/ui/Skeleton.tsx`（新增）

```tsx
interface SkeletonProps {
  variant?: 'text' | 'card' | 'circle' | 'rect';
  width?: string;
  height?: string;
  className?: string;
}

export default function Skeleton({ variant = 'text', width, height, className = '' }: SkeletonProps) {
  const variantStyles = {
    text: 'h-4 w-full',
    card: 'h-24 w-full',
    circle: 'h-12 w-12 rounded-full',
    rect: 'h-32 w-full',
  };

  return (
    <div
      className={`skeleton ${variantStyles[variant]} ${className}`}
      style={{ width, height }}
      aria-hidden="true"
    />
  );
}
```

**状态**：待创建

---

### 文件2：`apps/web/src/components/ui/PageSkeleton.tsx`（新增）

```tsx
import Skeleton from './Skeleton';

interface PageSkeletonProps {
  variant: 'today' | 'actions' | 'growth' | 'my' | 'chat' | 'article';
}

export default function PageSkeleton({ variant }: PageSkeletonProps) {
  const variants = {
    today: (
      <div className="space-y-4 p-4">
        <Skeleton variant="text" width="60%" />
        <Skeleton variant="text" width="40%" />
        <Skeleton variant="card" />
        <Skeleton variant="card" />
        <Skeleton variant="card" />
      </div>
    ),
    actions: (
      <div className="space-y-4 p-4">
        <div className="flex justify-center">
          <Skeleton variant="circle" />
        </div>
        <div className="grid grid-cols-3 gap-2">
          <Skeleton variant="card" height="60px" />
          <Skeleton variant="card" height="60px" />
          <Skeleton variant="card" height="60px" />
        </div>
        <Skeleton variant="card" />
        <Skeleton variant="card" />
      </div>
    ),
    growth: (
      <div className="space-y-4 p-4">
        <div className="flex items-center gap-3">
          <Skeleton variant="circle" />
          <div className="flex-1 space-y-2">
            <Skeleton variant="text" width="40%" />
            <Skeleton variant="text" width="30%" />
          </div>
        </div>
        <Skeleton variant="card" />
        <Skeleton variant="card" />
      </div>
    ),
    my: (
      <div className="space-y-4 p-4">
        <div className="flex items-center gap-3">
          <Skeleton variant="circle" />
          <div className="flex-1 space-y-2">
            <Skeleton variant="text" width="50%" />
            <Skeleton variant="text" width="30%" />
          </div>
        </div>
        <Skeleton variant="card" />
        <Skeleton variant="card" />
        <Skeleton variant="card" />
      </div>
    ),
    chat: (
      <div className="space-y-4 p-4">
        <div className="flex gap-2">
          <Skeleton variant="circle" />
          <Skeleton variant="card" width="70%" height="60px" />
        </div>
        <div className="flex gap-2 justify-end">
          <Skeleton variant="card" width="70%" height="60px" />
          <Skeleton variant="circle" />
        </div>
      </div>
    ),
    article: (
      <div className="space-y-4 p-4">
        <Skeleton variant="text" width="80%" />
        <Skeleton variant="text" width="60%" />
        <Skeleton variant="rect" />
        <Skeleton variant="text" />
        <Skeleton variant="text" />
        <Skeleton variant="text" width="90%" />
      </div>
    ),
  };

  return (
    <div className="page-skeleton" aria-label="页面加载中">
      {variants[variant]}
    </div>
  );
}
```

**状态**：待创建

---

### 文件3：`apps/web/src/App.tsx`

#### 修改项1：路由切换添加过渡
```tsx
// 修改前
<Route path="/today" element={<TodayPage />} />

// 修改后
<Route
  path="/today"
  element={
    <Suspense fallback={<PageSkeleton variant="today" />}>
      <TodayPage />
    </Suspense>
  }
/>
```

#### 修改项2：RouteFallback使用骨架屏
```tsx
// 修改前
const RouteFallback = () => (
  <div style={{ textAlign: 'center', padding: '40px' }}>
    <p>页面加载中...</p>
  </div>
);

// 修改后
const RouteFallback = () => (
  <PageSkeleton variant="today" />
);
```

**状态**：待修改

---

### 文件4：`apps/web/src/index.css`

#### 修改项1：添加prefers-reduced-motion支持
```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
  
  .skeleton {
    animation: none;
    background: var(--paper-warm);
  }
}
```

#### 修改项2：添加全局focus-visible样式
```css
:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

button:focus-visible,
a:focus-visible,
[role="button"]:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}
```

#### 修改项3：添加路由切换过渡
```css
.page-transition-enter {
  opacity: 0;
  transform: translateY(8px);
}

.page-transition-enter-active {
  opacity: 1;
  transform: translateY(0);
  transition: opacity 200ms, transform 200ms;
}

.page-transition-exit {
  opacity: 1;
}

.page-transition-exit-active {
  opacity: 0;
  transition: opacity 200ms;
}
```

**状态**：待修改

---

### 文件5：`apps/web/src/components/ui/ErrorBoundary.tsx`（新增）

```tsx
import { Component, type ReactNode } from 'react';

interface Props {
  children: ReactNode;
  fallback?: ReactNode;
}

interface State {
  hasError: boolean;
  error?: Error;
}

export default class ErrorBoundary extends Component<Props, State> {
  constructor(props: Props) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError(error: Error): State {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    console.error('ErrorBoundary caught:', error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return this.props.fallback || (
        <div className="p-8 text-center">
          <h2 className="text-lg font-bold mb-2">页面出错了</h2>
          <p className="text-sm text-ink-muted mb-4">请刷新页面重试</p>
          <button
            onClick={() => window.location.reload()}
            className="px-4 py-2 bg-accent text-paper"
          >
            刷新页面
          </button>
        </div>
      );
    }

    return this.props.children;
  }
}
```

**状态**：待创建

---

### 文件6：各页面添加aria-label

#### TodayPage.tsx
```tsx
// 为所有IconButton、无文字按钮添加aria-label
<IconButton icon={RefreshCw} ariaLabel="刷新内容" onClick={handleRefresh} />
```

#### ActionsPage.tsx
```tsx
// 为打卡按钮添加aria-label
<button aria-label="每日打卡" onClick={handleCheckIn}>...</button>
```

#### ChatPage.tsx
```tsx
// 为发送按钮添加aria-label
<button aria-label="发送消息" onClick={handleSend}>...</button>
```

（其他页面类似处理）

**状态**：待修改

---

## 3. 可访问性检查清单

| 检查项 | 目标 | 验证方式 |
|--------|------|---------|
| aria-label | 所有IconButton、无文字按钮 | 代码审查 |
| focus-visible | 所有交互元素 | 键盘Tab测试 |
| 颜色对比度 | --ink-muted在--paper上≥4.5:1 | 浏览器DevTools |
| 字体大小 | 最小10px | 代码审查 |
| 减少动画 | prefers-reduced-motion支持 | 系统设置测试 |

---

## 4. 执行步骤

1. [ ] 创建 `Skeleton.tsx`
2. [ ] 创建 `PageSkeleton.tsx`
3. [ ] 创建 `ErrorBoundary.tsx`
4. [ ] 修改 `App.tsx` - Suspense fallback使用骨架屏
5. [ ] 修改 `index.css` - 添加prefers-reduced-motion + focus-visible + 路由过渡
6. [ ] 修改各页面 - 添加aria-label
7. [ ] 修改 `components/ui/index.ts` 导出新组件
8. [ ] 执行 `npm.cmd run check`
9. [ ] E2E复验
10. [ ] 键盘Tab导航测试

---

## 5. 验收标准

- [ ] `npm.cmd run check` 全绿
- [ ] E2E测试通过
- [ ] 键盘Tab导航可用，焦点可见
- [ ] 减少动画模式下无动画
- [ ] 所有IconButton有aria-label
- [ ] 页面加载时显示骨架屏而非文字

---

## 6. 执行记录

| 时间 | 动作 | 结果 | 备注 |
|------|------|------|------|
| 待记录 | 待记录 | 待记录 | 待记录 |

---

## 7. 风险与回退

| 风险 | 缓解措施 |
|------|---------|
| 骨架屏与真实内容差异大导致闪烁 | 骨架屏结构尽量接近真实内容布局 |
| focus-visible样式与复古风格冲突 | 使用accent色outline，保持2px宽度 |
| 路由过渡动画影响性能 | 使用CSS transition而非JS动画 |

---

## 8. 下一步

批次6完成后，进入批次7：架构层（响应式+暗色模式+错误边界）
