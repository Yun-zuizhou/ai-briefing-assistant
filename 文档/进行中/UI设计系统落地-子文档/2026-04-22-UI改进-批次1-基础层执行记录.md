# UI改进-批次1-基础层执行记录

- 文档定位：`2026-04-22-UI设计系统落地与页面层尾差清理.md` 的子文档（线A-批次1）
- 建立日期：2026-04-22
- 完成日期：2026-04-24
- 批次目标：Token体系修正 + 根容器适配
- 复验方式：`npm.cmd run task:verify:proof`
- 执行状态：已完成

---

## 1. 本批次完成内容

### 1.1 Token体系修正

已更新：

1. `apps/web/src/styles/design-tokens.ts`
2. `apps/web/src/styles/design-tokens.css`
3. `apps/web/tailwind.config.js`

核心变化：

1. 字号体系整体上调（`base` 从 `12px` 等级提升到 `14px` 等级）。
2. 补齐 `accent dark` 与 `surface` 语义色（`surface-raised`、`surface-soft`）。
3. 补齐软阴影 token（`shadow-soft`）。
4. 扩展 spacing（新增 `3xl/4xl`）。
5. 统一语义文本尺寸（`body/meta/micro` 可读性提升）。

### 1.2 根容器与移动外壳收口

已更新：

1. `apps/web/src/index.css`
2. `apps/web/src/styles/shell.css`
3. `apps/web/src/components/layout/PageLayout.tsx`

核心变化：

1. 根容器改为移动壳优先：`#root` 收口为 `width: min(100%, 430px)`。
2. 桌面端默认仅做移动预览，不再依赖“假状态栏/装饰角”。
3. 降低纸纹噪点强度，减少视觉干扰。
4. `app` 容器统一 `min-height: 100dvh`。

---

## 2. 门禁复验结果

执行时间：2026-04-24 04:30:38 ~ 04:31:19（UTC+08:00）

执行命令：

```powershell
npm.cmd run task:verify:proof
```

结果：

1. `npm.cmd run check`：通过
2. Python：`76 passed`
3. Workers：`18 files / 102 tests`

摘要文件：

1. `var/logs/gate-check/latest-summary.md`
2. `var/logs/gate-check/20260424-043038-summary.md`

---

## 3. 变更结论

1. 批次1目标已完成，并通过主门槛复验。
2. UI基础层已具备继续推进组件层（批次2）的前置条件。
3. 当前未引入新依赖，未改动路由结构与业务逻辑。

---

## 4. 下一步（批次2）

按计划进入：`UI改进-批次2-组件层执行记录.md`

优先事项：

1. Button/Card/Input/Tag 变体收口。
2. 页面高频内联样式开始迁移到组件与语义类。
3. 每完成一个页面组后执行 `npm.cmd run task:verify:proof`。
