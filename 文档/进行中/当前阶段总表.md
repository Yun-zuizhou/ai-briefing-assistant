# 当前阶段总表

- 项目名称：AI简报助手（时代与我）
- 当前阶段：阶段十五（Cloudflare Workers 迁移）代码已合入，进入产品层尾差清理；2026-04-27 已完成五个重点页阶段 1 低风险前端收口，并启动阶段 2 前后端契约收口
- 当前主线：Actions / Journal / Today 三个阶段 2 契约收口已完成，已进入阶段 2 收尾验收与页面人工验收准备
- 更新日期：2026-04-27

## 1. 当前阶段目标

1. 保持 `npm.cmd run check` 全绿
2. 维持正式仓库边界、正式接口边界和真实运行入口不回退
3. 继续把页面层最影响体感的尾差收掉
4. 在主链路跑通后持续补齐次要链路，直到“完整链路”可验证
5. **已完成**：Cloudflare Workers 迁移（后端 + 前端均已上线）

## 2. 当前阶段完成口径

1. 正式仓库边界已收紧，核心目录、契约文档、归档区已经分开
2. `mock-data` 已迁入 `apps/web/demo/mock-data`
3. `npm.cmd run check` 已成为唯一准入门槛；当前测试数字与最近一次复验结果，以 `当前测试与验收总表.md` 为准
4. D1 / 本地双环境读取链已经稳定
5. `HistoryBrief` 已支持真实历史报告对象列表和按 `report_id` 打开详情
6. `Actions` 已支持真实后端打卡状态与 `POST /actions/check-in`
7. `Growth` 关键词已从系统噪音词改成更接近用户画像的关键词
8. 内容详情已具备最小可读正文兜底
9. Pydantic / SQLAlchemy deprecation warnings 已清零
10. 前端大包告警已清掉，路由页已拆分为懒加载
11. 内容层相关模块已可单独导入测试，不再依赖包级聚合导入顺序
12. **新增**：Cloudflare Workers 迁移已完成，正式 API 面已覆盖 `auth / dashboard / actions / content / preferences / chat / reports / favorites / notes / todos / history / feedback / system` 等核心路由
13. **新增**：前端已部署到 Cloudflare Pages
14. **新增**：`chat/reclassify` D1 分支会话持久化缺陷已修复
15. **新增**：`actions/check-in` streak 回归问题已修复
16. **新增**：已补“对话关注AI -> interests写入 -> today推荐命中AI”自动化回归用例
17. **新增**：中断恢复后已复验 `workers` 类型检查通过
18. **新增**：中断恢复后已复验 `python tools/scripts/verify_d1_behavior_mainline.py` 通过
19. **新增**：`npm.cmd run check` 已恢复并保持全绿，具体结果以 `当前测试与验收总表.md` 为准
20. **新增**：已落地 `0010_add_supporting_chain_tables.sql`，补齐调度、结果、观测与回放等支撑链事实表
21. **新增**：已补“机会 -> 待办 -> 跟进 -> 结果”强绑定（`related_* + opportunity_follows + opportunity_execution_results`）
22. **新增**：已补“推送设置 -> 调度事实层”链路（`briefing_schedules + briefing_dispatch_logs`）
23. **新增**：已新增 `/api/v1/system/*` 支撑链可观测接口（链路健康、采集运行、AI加工运行、操作日志、回放任务）
24. **新增**：统一门槛与 D1 主链验证已连续通过两轮（非一次性通过）
25. **新增**：已收紧 Workers `chat/execute` 与 `chat/reclassify` 的失败口径，移除“虚拟成功回退”，写失败改为显式失败返回
26. **新增**：已统一 D1 `active/ACTIVE` 状态读取口径（机会、兴趣、调度），减少历史数据大小写导致的漏读
27. **新增**：已补齐 Workers 路由测试（`actions/content/favorites/history/notes/preferences/reports/system/todos`），形成主链/次链/支撑链全部核心路由最小回归集合
28. **新增**：Workers 测试已复验通过，具体结果以 `当前测试与验收总表.md` 为准
29. **新增**：已新增并验证 `check:workers` 命令（`workers` 类型检查 + 测试）
30. **新增**：已新增并验证 `preflight:workers-sync`（`workers` 门槛 + `D1` 主链验证一体化预检）
31. **新增**：已收口本地联调入口，`npm.cmd run dev` 默认走 Workers（`8787`），并保留 `npm.cmd run dev:python` 回退入口
32. **新增**：已把 `setup` 与默认 Workers 运行链对齐（自动检查/安装 Workers 依赖）
33. **新增**：已新增 `check:full`（灰度全链路门槛，`check + workers`）
34. **新增**：已新增 `check:stage` 作为阶段内推荐默认验收口径（等价 `check:full`）
35. **新增**：`check:stage` 已实测通过，可作为当前阶段默认验收口径执行
36. **新增**：已完成运行层目录归位：`apps/web`、`apps/edge-worker`、`tools/scripts`、`infra/cloudflare/d1`
37. **新增**：已抽出 `packages/contracts/src/page-data.ts` 作为前后端共享页面契约单一源
38. **新增**：本地 SQLite / 调试日志已收口到 `var/local` 与 `var/logs`
39. **新增**：根目录历史 Cloudflare 占位入口已归档，正式 Cloudflare 入口只保留 `apps/edge-worker/wrangler.toml`
40. **新增**：`npm.cmd run db:d1:prepare` 已在新 D1 目录结构下通过，seed 生成路径已切到 `infra/cloudflare/d1/seeds/generated`
41. **新增**：登录页已加入 3 类测试邮箱快速切换（新用户新邮箱 / `test@example.com` / `show@example.com`）
42. **新增**：云端 D1 测试身份已完成重排，`test@example.com` 继续承接完整主链数据，`show@example.com` 已形成独立展示数据
43. **新增**：`apps/web` 已重新部署到 Cloudflare Pages，正式域名已切到最新 bundle
44. **新增**：线上最新前端 bundle 已复验与当前 session 鉴权和 Workers 路由口径对齐
45. **新增**：前端页面分层清理已启动，`Marks / DesignPreview / StylePreview / DecorPreview` 已从正式产品路径剥离到 `/demo/*`
46. **新增**：正式 `AppContext` 已收缩为“登录壳 + UI 状态”，不再承接兴趣、收藏、故事、推送时间等业务真源
47. **新增**：demo 页面已挂独立 `DemoAppProvider`，demo 状态与正式前端状态已分离
48. **新增**：demo 组件与 demo 数据已完成目录级分区（`src/demo/*`）；历史 compat 壳页已完成物理清理，最新前端已重新部署到 Pages 正式域名
49. **新增**：已完成一次系统项目进度整理与归档清理：
   - 阶段十四旧主线文档已转入 `文档/归档`
   - Railway/旧评估/旧汇报文档已降级到 `文档/过时`
   - 当前执行入口已改由新的归档执行主线文档承接
50. **新增**：已完成一次前端目录结构二次清理：
   - `apps/web` 历史预览 HTML、旧 `vercel.json`、旧设计稿已归档到 `_archive_workspace`
   - `app/README.md`、`apps/web/README.md` 与 `apps/web/src/demo/README.md` 已补齐目录角色说明
   - `var` 散落 wrangler 日志已并入 `var/logs`
51. **新增**：上述前端目录结构整理后，`npm.cmd --prefix apps/web run build` 已复验通过
52. **新增**：已按“当前项目资产统一装箱”规则继续区分当前项目与其他项目资产：
   - `_archive_workspace/design-and-drafts` 下当前项目资产已集中到 `AI简报助手（时代与我）/`
   - `文档/学术答辩` 已拆分出 `AI简报助手（时代与我）/` 与 `其他项目/`
53. **新增**：当前主门槛已在当前机器权限下再次复验通过，具体结果已写入 `当前测试与验收总表.md`
54. **新增**：已新增 `当前测试与验收总表.md`，测试数字当前改由单一事实源维护
55. **新增**：已把“突破升级触发器”写入规则层与协作协议层，任务结束后默认检查 5 个升级问题
56. **新增**：Workers 受保护路由已移除 `user_id / x-user-id / x-user-email` 旧身份透传，正式用户来源已收口到服务端 session
57. **新增**：Workers CORS 与本地 `wrangler` 旧身份透传配置已清理，不再给 `X-User-*` 正式入口留白
58. **新增**：Workers 自动化测试已切到 cookie session 认证上下文，不再使用 `?user_id=1` / `x-user-id`
59. **新增**：Workers 第一批资源级 `403` 已落地，`todos / notes / favorites / reports / chat sessions / system summary tasks` 不再把越权访问统一混成 `404`
60. **新增**：`GET /api/v1/system/replay-tasks` 已补正式鉴权与用户范围限制，`POST /api/v1/system/replay-tasks` 已补操作日志归属检查
61. **新增**：`POST /api/v1/system/ingestion-runs` 与 `POST /api/v1/system/ai-processing-runs` 已补 internal token 最小鉴权，不再对普通请求开放
62. **新增**：主门槛已再次复验通过，Workers 自动化覆盖已继续扩充；具体结果以 `当前测试与验收总表.md` 为准
63. **新增**：`apps/edge-worker/.dev.vars.example`、`apps/edge-worker/README.md` 与 `setup` 自动补文件已落地，`INTERNAL_API_TOKEN` 当前已有正式本地配置入口
64. **新增**：已核验真实线上对象为默认 Worker `ai-briefing-assistant`，并已完成 `INTERNAL_API_TOKEN` 生产 secret 配置；`env.production` 误导口径已清理
65. **新增**：已新增并验证 `npm.cmd run preflight:workers-config`，可补充核验本地 `.dev.vars` 与默认 Worker secret 是否齐备
66. **新增**：`todo / note / report / replay-task` 的第二批资源级 `403` 回归测试已补齐，Workers 权限覆盖已再次扩充并复验通过
67. **新增**：`e2e/app-mainline.spec.ts` 已在本轮复验通过，正式鉴权烟雾与主线浏览器交互已具备一轮自动化证明；具体结果以 `当前测试与验收总表.md` 为准
68. **新增**：`auth-guard.route.test.ts` 已在本轮复验通过，主要受保护 API 路由的未登录 `401` 已具备批量自动化证明；具体结果以 `当前测试与验收总表.md` 为准
69. **新增**：`ArticlePage / HotTopicsPage` 面向用户暴露的“过渡态 / 真实接口过渡态 / 统一引用”等研发态文案已完成一轮产品化清理，并已通过前端构建验证
70. **新增**：`ChatPage / CollectionsPage / HistoryLogsPage` 中直接暴露内部引用键的展示点已继续清理，页面跳转与上下文能力保持不变，前端构建已连续两轮通过
71. **新增**：`ChatPage` 识别过程元信息已改成用户表达，`Weekly / Monthly / Annual Report` 的可信度展示已完成中文化，前端构建已连续三轮通过
72. **新增**：已新增“核心页面人工验收与截图证据清单”，并已把 `e2e/app-mainline.spec.ts` 断言同步到当前正式 UI，浏览器级主文件已于 2026-04-19 复验通过（具体数字以 `当前测试与验收总表.md` 为准）
73. **新增**：已完成高频运行 / 权限 / 预检入口的一键命令收口，根级已建立 `task:init / task:start / task:verify / task:access:* / task:data:*` 等统一入口
74. **新增**：已建立 `文档/命令规范/` 专题目录，并完成一轮文档区物理清理：该归档的已归档，该过时的已过时；`app` 迁移子线也已转入归档候选资料
75. **新增**：`content/detailState` 与 `dashboard/processingStage` 的共享契约语义已从 `transitional` 统一收口为 `partial`，当前表达改为“部分字段仍在继续补齐”，不再继续把用户可见状态命名为“过渡态”
76. **新增**：月报页已移除固定伪趋势柱，改为基于真实 `previous/current` 数据的前后值对比；报告可信度表达继续以 `dataQuality` 为准
77. **新增**：`ChatPage / HistoryLogsPage` 的静态契约断言已重新对齐当前正式 UI 文案与结构，相关 Python 契约测试已复验通过
78. **新增**：`npm.cmd run check` 已于 2026-04-22 在当前机器完整复验通过；当前明细结果以 `当前测试与验收总表.md` 为准
79. **新增**：`npx.cmd playwright test e2e/app-mainline.spec.ts` 已于 2026-04-22 复验通过，主线浏览器交互当前再次具备自动化证明
80. **新增**：`intentLogger` 默认本地持久化已退出正式前端路径；当前 `localStorage` 继续只保留 `Chat` 缓存等界面级状态，不再为解析日志提供默认正式存储
81. **新增**：`/api/v1/system/summary-tasks/:id/start|complete|fail` 已落地 internal 执行入口，`summary task` 当前已具备 `queued -> running -> succeeded/failed` 的最小状态流转接口
82. **新增**：上述 `summary task` 执行链已补齐 Workers 路由测试并并入主门槛；`npm.cmd run check` 已于 2026-04-22 再次复验通过，当前 Workers 明细以 `当前测试与验收总表.md` 为准
83. **新增**：2026-04-24 发现并修复 `tests/test_dashboard_today.py` 的 3 处断言漂移（`content_ref` 契约类型与 Chat UI 字符串断言），同日复验 `npm.cmd run check` 通过；当前数字以 `当前测试与验收总表.md` 为准
84. **新增**：已新增门禁摘要命令 `npm.cmd run task:verify:proof`（落地 `var/logs/gate-check/latest-summary.md`），并将测试文档更新流程改为“先跑门禁并产出摘要，再写通过结论”
85. **新增**：2026-04-24 已执行 `npm.cmd run check:proof:strict`，确认 `check + check:workers` 双命令摘要证据均为通过；阶段收口可采用该模式做更严格留痕
86. **新增**：UI 设计系统落地线A-批次1（基础层）已于 2026-04-24 完成（token 与移动壳收口），并通过同日 `npm.cmd run task:verify:proof` 复验；批次2（组件层）可启动
87. **新增**：2026-04-27 已完成 Chat 页阶段 1 最小前端收口：消息列表不再默认只显示最近 4 条，消息面板改为随页面主内容区滚动，来源上下文不再直接暴露内部引用键；本轮未改 Workers、contracts 或 D1 schema，并已复验通过，具体结果以 `当前测试与验收总表.md` 为准
88. **新增**：2026-04-27 已完成 Actions 页阶段 1 最小前端收口：首屏改为“今天先推进这 1 件”，打卡与统计下沉为轻量摘要，后续跟进上移到收藏待处理之前，待办来源和优先级改为中文自然表达；本轮未改 Workers、contracts 或 D1 schema，并已复验通过，具体结果以 `当前测试与验收总表.md` 为准
89. **新增**：2026-04-27 已补“个人沉淀闭环与页面职责边界”方向判断：下一步 Journal 不按普通记录列表页修补，而按“我说过的 / 我推进过的 / 我留下的 / 我周期性理解的”沉淀闭环收口；阶段 1 默认只用现有 API，不改 Workers、contracts 或 D1 schema
90. **新增**：2026-04-27 已完成 Journal 页阶段 1 最小前端收口：页面从“今日叙事 + 成长轨迹 + 想法记录”改为“最近沉淀 / 我刚说过的 / 我推进过的 / 我留下的 / 长期回看”，移除 `AI生成` 前端假状态，仅读取现有正式 API，不改 Workers、contracts 或 D1 schema；主门槛、门禁摘要与浏览器级主路径已复验通过，具体结果以 `当前测试与验收总表.md` 为准
91. **新增**：2026-04-27 已完成 Today / 简报页阶段 1 最小前端收口：首屏从“目录式总述”改为“今天先看 / 今天先做”的重点判断，过渡性摘要文案在前端呈现层降噪，值得知道与值得行动列表收束为高优先级内容，推荐、追问、记录和转待办入口继续基于现有 `dashboard/today` 正式字段组织；本轮未改 Workers、contracts 或 D1 schema，并已复验通过，具体结果以 `当前测试与验收总表.md` 为准
92. **新增**：2026-04-27 已完成 My / 我的页阶段 1 最小前端收口：页面从“成长摘要 + 退出登录混排”改为“个人沉淀入口 + 入口分发 + 底部账号区”，退出登录下沉为账号动作，成长与画像、沉淀回看、系统与支持按职责分组；本轮未改 Workers、contracts 或 D1 schema，并已复验通过，具体结果以 `当前测试与验收总表.md` 为准
93. **新增**：2026-04-27 已完成阶段 2 前后端契约收口评估：确认 Actions 应先扩展 `ActionsOverviewData` 的 `topPriority / suggestedNextActions`，Journal 应新增 `JournalOverviewData` 与 `GET /api/v1/journal/overview`，Today 应先校正真实 `briefings.payload` 与文档中 `result_payload` 的口径差异，再扩展 `TodayPageData` 的 `leadItem / extensionSlots`；本轮为文档评估，不更新测试数字
94. **新增**：2026-04-27 已完成 Actions 主行动契约收口：`GET /api/v1/actions/overview` 在不新增 endpoint、不改 D1 schema 的前提下扩展 `topPriority / suggestedNextActions`，Workers 统一生成今日主行动与后续建议，前端优先消费正式契约并保留旧字段兜底；已复验通过，具体结果以 `当前测试与验收总表.md` 为准
95. **新增**：2026-04-27 已完成 Journal Overview 聚合契约收口：新增 `JournalOverviewData` 与 `GET /api/v1/journal/overview`，Workers 聚合 `notes / todos / favorites / history_entries / opportunity_follows / reports / interests` 形成记录页正式读侧，前端优先消费新接口并保留旧多接口兜底；本轮未改 D1 schema，并已复验通过，具体结果以 `当前测试与验收总表.md` 为准
96. **新增**：2026-04-27 已完成 Today 每日结果对象契约收口：`TodayPageData` 扩展可选 `leadItem / dailyAngle / freshness / extensionSlots`，`GET /api/v1/dashboard/today` 优先消费真实 `briefings.payload`，缺失时继续使用热点与机会即时聚合兜底；本轮未新增 endpoint，未改 D1 schema，并已复验通过，具体结果以 `当前测试与验收总表.md` 为准
97. **新增**：2026-04-27 已新增阶段 2 契约收口验收与遗留问题清单，明确当前不继续横向扩新接口；本轮发现并修复 Today `briefing` 类型主判断误进文章详情的 P1 小缺口，并已复验通过，具体结果以 `当前测试与验收总表.md` 为准；后续优先执行移动端人工验收与 `briefings.payload` 生产质量抽样

## 3. 当前唯一准入门槛

- `npm.cmd run check`

当前只承认这一条门槛，不再并列维护：

- `pytest tests/test_api_mainline.py -q`
- `npm.cmd run test:api-mainline`

补充说明（辅助专项门槛，不替代主门槛）：

- `npm.cmd run check:workers`
- `npm.cmd run preflight:workers-sync`
- `npm.cmd run check:full`（灰度全链路）
- `npm.cmd run check:stage`（当前阶段推荐默认）

### 3.1 灰度并入主门槛退出条件

1. `check:stage` 连续 7 天稳定通过，无新增 P0/P1 回归。
2. Workers 测试在已覆盖全部核心路由基础上，继续补深度场景断言后仍稳定通过。
3. `check:stage` 在本地与无人值守环境均可稳定执行，不依赖人工提权。
4. `preflight:workers-sync` 连续通过，D1 认证与绑定无漂移。
5. 双后端影子比对达到 48h 无 P0/P1 语义偏差。

## 4. 当前唯一标准命令

1. `npm.cmd run setup`
2. `npm.cmd run dev`
3. `npm.cmd run check`

补充说明：

- 高频事项已在 `文档/命令规范/` 收口为 `task:*` 一键入口
- 但项目级基础口径仍只认 `setup / dev / check`

## 5. 当前主要风险

1. 页面级人工验收仍不足，当前更多是接口和技术链路已经收稳
2. 正式鉴权已完成第三轮收口，但资源级 `403` 仍未全覆盖，生产人工验收、secret 轮换流程与最终传输契约说明仍未完全补齐
3. Chat 仍需继续严格收口为单主线写入，避免前端失败回退再次混回正式链
4. 仍需在人工验收中补“关注AI闭环”的截图级证据
5. 报告可信度口径尚未完全收口，`insufficient_data` 与关键指标定义仍需补齐
6. 支撑链目前是“最小事实层已落位”，自动调度执行器仍需后续补全
7. Workers 与 Python 仍处于迁移兼容阶段，尚未完成 48h 影子比对闸门
8. Workers 自动化已覆盖全部核心路由，但当前仍未并入唯一主门槛 `check`，需要继续补深度场景断言并判断并入口径
9. Python `app/` 仍在根目录，尚未迁入 `apps/api`；该迁移评估已启动，当前结论是“允许先补兼容层，不允许直接一次性物理迁移”，避免误伤离线脚本、验证脚本与 `check` 稳定性
10. 当前测试身份链已切到 session cookie 正式上下文，但 3 类测试邮箱的生产人工验收、资源级权限校验与 internal token 轮换流程仍需补齐
11. 正式页面里仍有少量内部字段展示点未完全清掉，当前已先收 `ArticlePage / HotTopicsPage / ChatPage / CollectionsPage / HistoryLogsPage`，后续仍需按同样标准继续排查
12. Journal 页已按个人沉淀闭环完成阶段 1 前端收口，并已完成 `journal/overview`、`packages/contracts` 的阶段 2 聚合契约收口；后续如继续增强，应优先围绕记录编辑、来源追溯和周期回看字段做最小契约评估
13. Today / 简报页已完成阶段 1 前端收口与阶段 2 每日结果对象契约收口；真实 D1 字段已按 `briefings.payload` 消费，后续不得按未落地的 `briefings.result_payload` 字段直接开工
14. 五个重点页的阶段 1 低风险前端收口已完成；阶段 2 已完成 Actions 主行动契约、Journal Overview 聚合契约与 Today 每日结果对象契约，并已进入验收定责；下一步只修 P0/P1 小缺口，避免继续一次性扩大页面契约范围

## 6. 长期候选项

当前需要明确：

1. `Cloudflare Pages Functions` 迁移不是当前执行主线。
2. 当前正式在线主线仍是：
   - `apps/web`
   - `apps/edge-worker`
   - `infra/cloudflare/d1`
3. 原《长期目标-迁移到Pages-Functions》已退出当前工作面，现已转入 `文档/过时/`。
4. 若未来重新评估同域 API 方案，应以新的主线文档重新立项，而不是继续沿用旧长期目标文档。

## 7. 当前下一步顺序

### 产品层主线（当前进行中）

1. 第一优先：继续补“身份与权限链”剩余收口，扩大资源级 `403` 覆盖并完成生产人工验收、internal token 轮换流程与正式传输契约说明
2. 第二优先：切断 Chat 前端本地正式写回，把失败分支收敛到显式失败或草稿态
3. 第三优先：收紧前端本地状态边界，清掉 `localStorage` 身份真源和正式业务真源角色
4. 第四优先：修报告可信度口径，补 `insufficient_data` 与关键指标真实定义
5. 第五优先：完成 `Today / Actions / Growth / My / Weekly` 人工验收与截图级证据补齐
6. 第六优先：把 `/api/v1/system/*` 接到真实调度与加工任务（从“可观测接口”升级到“真实执行链”）
7. 第七优先：推进双后端影子比对并满足 48h 无 P0/P1 回归闸门
8. 第八优先：按验收结果继续收页面级缺陷，不回到收益排序或对象扩面
9. 第九优先：如需重启 `app/` 迁移子线，先回到 `文档/归档/AI简报助手-app迁移评估与正式态候选/` 再决定是否恢复执行
10. 第十优先：用 3 类测试邮箱补齐生产环境人工验收与截图级证据
11. 第十一优先：继续清 demo 历史资产的归档/删除策略，避免 `src/demo` 继续无限膨胀

### Cloudflare Workers 迁移主线（阶段四已完成）

**决策依据**：放弃 Railway 方案，统一使用 Cloudflare 平台（D1 + Workers + Pages）

**迁移阶段**：
1. ✅ 阶段零：项目初始化（已完成）
2. ✅ 阶段一：P0 API 迁移（已完成）
3. ✅ 阶段二：P1 API 迁移（已完成）
4. ✅ 阶段三：P2/P3 API 迁移（已完成）
5. ✅ 阶段四：前端适配 + 测试（已完成）

**阶段零完成内容**：
- ✅ 创建 `workers/` 目录结构
- ✅ 初始化 TypeScript + Hono 项目
- ✅ 配置 `wrangler.toml`（绑定 D1 数据库）
- ✅ 创建类型定义文件（与前端一致）
- ✅ 配置 CORS 中间件
- ✅ 创建 D1 数据库查询工具
- ✅ 创建路由文件结构（dashboard/actions/content）
- ✅ 安装依赖并通过类型检查

**阶段一完成内容**：
- ✅ 完善 `dashboard/today` 业务逻辑（兴趣匹配、排序算法）
- ✅ 完善 `actions/overview` 业务逻辑（待办分组、打卡状态）
- ✅ 完善 `content/by-ref` 业务逻辑（热点/机会详情）
- ✅ 添加 `preferences/growth-overview` 端点
- ✅ 本地开发服务器启动成功（http://127.0.0.1:8787）

**阶段二完成内容**：
- ✅ 添加 `chat/recognize` 端点（意图识别）
- ✅ 添加 `chat/execute` 端点（对话执行）
- ✅ 添加 `chat/sessions` 端点（会话列表）
- ✅ 添加 `chat/sessions/:id/messages` 端点（消息列表）
- ✅ 添加 `reports/` 端点（报告列表）
- ✅ 添加 `reports/weekly` 端点（周报）
- ✅ 添加 `reports/monthly` 端点（月报）
- ✅ 类型检查通过

**阶段三完成内容**：
- ✅ 添加 `preferences/interests` 端点（关注列表）
- ✅ 添加 `preferences/settings` 端点（通知设置）
- ✅ 添加 `preferences/profile` 端点（用户画像）
- ✅ 添加 `favorites/` 端点（收藏管理）
- ✅ 添加 `notes/` 端点（记录管理）
- ✅ 添加 `todos/` 端点（待办管理）
- ✅ 类型检查通过

**阶段四完成内容**：
- ✅ 更新前端 API 基础 URL 配置（.env, .env.production, vite.config.ts）
- ✅ 本地 API 端点测试通过
- ✅ 部署 Workers 到 Cloudflare（https://ai-briefing-assistant.aibriefing2026.workers.dev）
- ✅ 构建前端生产版本
- ✅ 部署前端到 Cloudflare Pages（https://ai-briefing-5d0.pages.dev）

**当前正式 API 路由面**：
- `/api/v1/auth/*`
  - 登录、注册、当前用户、退出
- `/api/v1/dashboard/*`
  - Today 聚合读侧
- `/api/v1/actions/*`
  - 行动概览、今日打卡
- `/api/v1/content/*`
  - 统一内容详情、热点/机会读侧
- `/api/v1/hot-topics`
  - legacy alias
- `/api/v1/opportunities`
  - legacy alias
- `/api/v1/preferences/*`
  - interests、settings、profile、growth-overview
- `/api/v1/chat/*`
  - recognize、execute、reclassify、sessions、messages
- `/api/v1/reports/*`
  - 列表、weekly、monthly、annual
- `/api/v1/favorites/*`
  - 收藏读写
- `/api/v1/notes/*`
  - 记录读写
- `/api/v1/todos/*`
  - 待办读写、完成
- `/api/v1/history/*`
  - 历史记录读写
- `/api/v1/journal/*`
  - 记录页聚合读侧
- `/api/v1/feedback/*`
  - 反馈提交
- `/api/v1/system/*`
  - 支撑链观测与执行事实接口

**技术选型**：
- 框架：Hono（轻量级 Web 框架）
- 语言：TypeScript
- 数据库：Cloudflare D1（已配置）

**详细方案**：见 [AI简报助手-阶段十五-Cloudflare-Workers迁移与正式化收口](/E:/python/杂谈-想法记录与实践/文档/归档/AI简报助手-阶段十五-Cloudflare-Workers迁移与正式化收口)

### 阶段切换条件

Cloudflare Workers 迁移主线已全部完成！

**生产环境**：
- Workers API: https://ai-briefing-assistant.aibriefing2026.workers.dev
- Pages 前端: https://ai-briefing-5d0.pages.dev

## 8. 当前相关文档

1. [项目核心总纲.md](/E:/python/杂谈-想法记录与实践/文档/项目核心总纲.md)
2. [项目核心子纲-阶段演进与归档索引.md](/E:/python/杂谈-想法记录与实践/文档/项目核心子纲-阶段演进与归档索引.md)
3. [技术现实清单.md](/E:/python/杂谈-想法记录与实践/文档/技术现实清单.md)
4. [当前测试与验收总表.md](/E:/python/杂谈-想法记录与实践/文档/进行中/当前测试与验收总表.md)
5. [2026-04-18-项目管理流程优化与测试事实源收口.md](/E:/python/杂谈-想法记录与实践/文档/进行中/2026-04-18-项目管理流程优化与测试事实源收口.md)
6. [2026-04-16-正式主线收口标准与接口契约设计.md](/E:/python/杂谈-想法记录与实践/文档/进行中/2026-04-16-正式主线收口标准与接口契约设计.md)
7. [2026-04-16-前端页面分层清理与正式路由剥离.md](/E:/python/杂谈-想法记录与实践/文档/进行中/2026-04-16-前端页面分层清理与正式路由剥离.md)
8. [2026-04-19-核心页面人工验收与截图证据清单.md](/E:/python/杂谈-想法记录与实践/文档/进行中/2026-04-19-核心页面人工验收与截图证据清单.md)
9. [2026-04-27-正式页面功能职责与问题梳理.md](/E:/python/杂谈-想法记录与实践/文档/进行中/2026-04-27-正式页面功能职责与问题梳理.md)
10. `文档/归档/AI简报助手-app迁移评估与正式态候选/`
11. [AI简报助手-阶段十五-Cloudflare-Workers迁移与正式化收口](/E:/python/杂谈-想法记录与实践/文档/归档/AI简报助手-阶段十五-Cloudflare-Workers迁移与正式化收口)
12. `文档/归档/AI简报助手-文档治理与工作区收口/`
13. [项目承接提示词-当前版.md](/E:/python/杂谈-想法记录与实践/文档/Agent协同经验/项目承接提示词-当前版.md)
14. [命令规范/常用一键命令.md](/E:/python/杂谈-想法记录与实践/文档/命令规范/常用一键命令.md)
15. [2026-04-27-个人沉淀闭环与页面职责边界方案.md](/E:/python/杂谈-想法记录与实践/文档/进行中/2026-04-27-个人沉淀闭环与页面职责边界方案.md)
16. [2026-04-27-阶段2前后端契约收口评估.md](/E:/python/杂谈-想法记录与实践/文档/进行中/2026-04-27-阶段2前后端契约收口评估.md)
17. [2026-04-27-阶段2契约收口验收与遗留问题.md](/E:/python/杂谈-想法记录与实践/文档/进行中/2026-04-27-阶段2契约收口验收与遗留问题.md)
