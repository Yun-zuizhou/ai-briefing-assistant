# Chat重要纠偏结果模板落地

- 文档状态：进行中
- 建立时间：2026-04-04
- 所属阶段：阶段二 - Today / 内容层设计与事实层收束
- 作用：记录本轮把重要纠偏结果从“共用通用正文结构”推进到“按结果类型使用更稳定模板”的这轮落地

---

## 1. 本轮目标

当前已经完成：

1. 用户表达去技术化
2. 消息视觉层级拆分
3. 辅助标签层降级

因此这轮不再继续压结构，而是开始处理另一类问题：

`不同纠偏结果不该继续共用过于通用的正文。`

---

## 2. 本轮已完成

### 2.1 已新增纠偏结果专用模板

当前在：

- [useChatLogic.ts](/E:/python/杂谈-想法记录与实践/prototype/src/hooks/useChatLogic.ts)

已新增：

- `buildReclassifyReply`

当前已根据纠偏类型分别输出不同模板：

1. `todo -> record_thought / fragmented_thought`
2. `note -> create_todo`
3. `todo / note -> chat_only`
4. `note -> retagged`

### 2.2 当前模板已能说清“旧对象怎么了”

当前重要纠偏结果已不再只说：

- 已完成纠偏

而是会更明确地说：

1. 我已经把这条改成……
2. 原来的内容我已经取消 / 保留
3. 新的内容我已经补好

这意味着当前“纠偏结果”已经开始像真正的用户回执，而不是系统状态摘要。

---

## 3. 本轮实际验收

本轮已通过：

1. `tests/test_dashboard_today.py`
2. `npm.cmd run test:api-mainline`
3. 默认 `pytest`
4. 根目录 `npm.cmd run build`
5. 根目录 `npm.cmd run lint`

当前结果：

1. `tests/test_dashboard_today.py` 为 `24 passed`
2. `npm.cmd run test:api-mainline` 为 `16 passed`
3. 默认 `pytest` 为 `30 passed, 1 skipped`
4. 根目录前端 `build` 通过
5. 根目录前端 `lint` 通过

---

## 4. 当前正确口径

当前应更新为：

1. Chat 重要纠偏结果当前已开始使用更稳定的结果模板
2. 不同纠偏类型已不再完全共用一套通用正文结构
3. 当前消息区已经不只是“主句更清楚”，而是关键结果开始拥有更贴近用户理解的专门表达

---

## 5. 下一步建议

顺着当前方向，下一步最合理的是：

1. 再判断是否需要把“普通执行结果”也逐步模板化
2. 避免纠偏结果很清楚，但普通结果仍然显得过于平
