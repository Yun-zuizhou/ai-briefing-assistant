# 接口级验证运行口径恢复

- 文档状态：进行中
- 建立时间：2026-04-04
- 所属阶段：阶段二 - Today / 内容层设计与事实层收束
- 作用：记录本轮把 `test_api_mainline.py` 从“环境噪音 / skip / 建库失败”重新收回为可稳定执行的正式接口级验证入口

---

## 1. 本轮要解决的问题

上一轮虽然已经把 Chat 后端纠偏留口落下来了，但接口级验证口径仍不稳定：

1. 默认 `pytest` 会跳过 `test_api_mainline.py`
2. 手动补运行时后，SQLite 测试库又起不来
3. 因此无法把“接口级验证已覆盖 reclassify”当成当前稳定口径

这轮的目标不是再写业务功能，而是把这条验收链真的跑通。

---

## 2. 本轮已完成

### 2.1 已补正式接口级验证入口

当前根目录已新增：

- [test-api-mainline.mjs](/E:/python/杂谈-想法记录与实践/scripts/test-api-mainline.mjs)

并在：

- [package.json](/E:/python/杂谈-想法记录与实践/package.json)

新增脚本：

- `npm.cmd run test:api-mainline`

当前这个入口会：

1. 自动带上项目运行所需的 `PYTHONPATH`
2. 直接执行 `tests/test_api_mainline.py`

### 2.2 已修复接口级测试建库基础设施

当前在：

- [test_api_mainline.py](/E:/python/杂谈-想法记录与实践/tests/test_api_mainline.py)

已完成：

1. 不再把测试库建在中文工作区路径下
2. 不再使用会导致 Windows 句柄占用的临时文件策略
3. 测试 SQLite 路径已改为系统临时目录根下的唯一文件
4. 已补句柄关闭与测试后清理

### 2.3 已修正 `favorites / history` 的统一返回口径

当前在：

- [favorites.py](/E:/python/杂谈-想法记录与实践/app/api/v1/favorites.py)
- [history.py](/E:/python/杂谈-想法记录与实践/app/api/v1/history.py)

已把响应模型收回到统一 `CamelModel` 口径。

这使得完整依赖环境下的接口返回现在能和当前前端、测试使用的 `contentRef` 契约重新对齐。

---

## 3. 本轮实际验收

本轮已实际通过：

1. `npm.cmd run test:api-mainline`
2. 默认 `pytest`
3. 根目录 `npm.cmd run build`
4. 根目录 `npm.cmd run lint`

当前结果：

1. `npm.cmd run test:api-mainline` -> `15 passed`
2. 默认 `pytest` -> `25 passed, 1 skipped`
3. 根目录前端 `build` 通过
4. 根目录前端 `lint` 通过

---

## 4. 当前正确口径

当前应更新为：

1. 默认 `pytest` 入口仍然可信
2. 完整依赖环境下，接口级验证入口已重新收口到 `npm.cmd run test:api-mainline`
3. `test_api_mainline.py` 当前已开始覆盖：
   - Today
   - Content
   - Preferences
   - Reports
   - Chat execute
   - Chat recognize
   - Chat reclassify
4. Chat 后端纠偏留口现在不只是代码存在，而是已有接口级回归锚点

---

## 5. 对当前主线的意义

这轮虽然没有扩功能，但它是主线内的重要推进：

1. 把“功能已写好但没有稳定验证入口”的风险收掉了
2. 让 Chat 后端纠偏留口真正进入可回归状态
3. 让后续继续扩纠偏对象范围时，有稳定的接口级测试基础可以依赖
