# 前端构建回归与依赖漂移标注

- 文档状态：进行中
- 建立时间：2026-04-03
- 所属阶段：阶段二 - Today / 内容层设计与事实层收束
- 作用：记录本轮接手同步时重新执行工程验收后发现的前端构建回归，避免继续沿用“build 已通过”的过期口径

---

## 1. 本轮重新核验了什么

本轮按当前阶段规则，重新执行了：

1. `npm.cmd run build`
2. `npm.cmd run lint`
3. `app/` 下 Python 全量语法编译
4. 默认 `pytest`
5. `pytest tests/test_api_mainline.py`

目的不是回头重做阶段一，而是确认当前核心文档中的工程健康判断是否仍然成立。

---

## 2. 本轮实际结果

### 2.1 仍然成立的部分

1. 前端 `lint` 通过
2. `app/` 下 Python 语法编译通过
3. 默认 `pytest` 结果仍为 `18 passed, 1 skipped`
4. 完整依赖环境下 `pytest tests/test_api_mainline.py` 仍为 `8 passed`

### 2.2 新确认的不成立部分

1. 前端 `build` 当前已失败

因此，当前工程健康状态不能再继续表述为：

`前端 build / lint 都通过`

更准确的表达应为：

`当前前端 lint 通过，但 build 已出现构建链回归；后端语法、默认 pytest、接口级验证仍然成立。`

---

## 3. 当前构建回归的直接表现

本轮实际执行：

- `npm.cmd run build`

当前脚本为：

- `tsc -b && .\\node_modules\\.bin\\vite.cmd build --configLoader native`

当前失败报错核心表现为：

1. Vite 已进入正式构建阶段
2. 但在 `vite:build-html` 阶段失败
3. 报错指出生成产物时收到了绝对路径形式的 `index.html`

当前应记录的最小事实是：

`这不是 Today / Chat 业务代码直接报错，而是前端构建链当前出现了回归。`

---

## 4. 当前已定位到的依赖漂移事实

本轮额外执行：

- `npm.cmd ls vite`

得到的当前本地事实是：

1. `node_modules` 中实际安装的 `vite` 为 `8.0.0-beta.18`
2. `package.json` 中当前声明仍是 `^8.0.0-beta.13`
3. 当前构建脚本仍依赖 `--configLoader native`

这说明当前至少存在一层需要被显式标注的现实：

`前端构建入口已经受到 Vite / rolldown 依赖组合变化的影响，当前不能再直接沿用旧文档中的“build 已通过”判断。`

---

## 5. 当前不应如何误判

不应误判成：

1. Today / 内容层主链突然整体失效
2. Chat / 内容详情 / 测试主链全部一起失效
3. 后端代码已经无法继续推进
4. `pytest` 和接口级验证结论全部失真

当前更准确的判断是：

1. 阶段二主线仍然是 `Today / 内容层设计与事实层收束`
2. Today 统一引用、统一详情、Chat 执行链路、接口级测试锚点仍然成立
3. 当前新增问题是前端默认构建验收入口出现独立回归

---

## 6. 对当前阶段判断的影响

这次回归会改变的是“工程健康口径”，不会改变当前唯一主线。

当前应这样理解：

1. 主线仍是 Today / 内容层
2. 但工程侧新增了一个必须承认的边界：
   - `npm.cmd run build` 当前不再是通过状态
3. 因此后续每轮如果继续涉及前端推进，不能再直接引用旧的“build 已通过”口径

---

## 7. 当前最合理的唯一修复动作

如果要恢复当前默认前端验收入口，最合理的唯一修复动作应是：

`先把 prototype 的 Vite 构建链重新锁回一组可验证的兼容组合，再重新执行 npm.cmd run build。`

在这一步之前，不应把当前回归误写成：

1. Today 业务逻辑回归
2. 内容层设计回归
3. 后端接口主链回归

---

## 8. 当前正确口径

本轮接手同步后的工程状态，当前已更新为：

1. 前端 `lint` 通过
2. 前端 `build` 已通过当前兼容脚本恢复
3. 当前恢复方式为：
   - `vite build --app --configLoader native`
4. 后端语法通过
5. 默认 `pytest` 入口可信，结果为 `18 passed, 1 skipped`
6. 接口级验证已继续补强，`pytest tests/test_api_mainline.py` 当前为 `10 passed`

因此，当前项目当前可恢复为：

`前端 build / lint 通过、后端语法通过、默认 pytest 入口可信、接口级验证通过。`

---

## 9. 本轮修复结果补记

本轮已实际完成：

1. `prototype/package.json` 的默认构建脚本改为：
   - `vite build --app --configLoader native`
2. 在当前本地依赖组合下，连续串行执行 `npm.cmd run build` 已恢复通过

当前更准确的工程判断应为：

1. 回归已被收住
2. 但修复依赖的是当前这条兼容构建脚本
3. 后续如果再升级 Vite / rolldown 组合，仍应重新验收，不应自动沿用“肯定稳定”口径
