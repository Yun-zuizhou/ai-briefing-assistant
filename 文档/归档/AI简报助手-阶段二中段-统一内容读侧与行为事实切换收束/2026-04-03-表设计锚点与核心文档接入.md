# 表设计锚点与核心文档接入

- 文档状态：进行中
- 建立时间：2026-04-03
- 所属阶段：阶段二 - Today / 内容层设计与事实层收束
- 目的：把既有表设计正式接入当前核心文档链路，避免后续推进脱离已有设计

---

## 1. 当前确认的既有表设计锚点

当前项目已经存在明确的表设计文档，不应另起炉灶。

当前最明确的表设计锚点为：

- [最小可用数据库表结构.sql](/E:/python/杂谈-想法记录与实践/文档/技术实现/最小可用数据库表结构.sql)

该文件已经提出的关键表包括：

1. `users`
2. `user_interests`
3. `user_settings`
4. `contents`
5. `opportunities`
6. `briefings`
7. `reports`
8. `todos`
9. `favorites`
10. `opportunity_follows`
11. `notes`
12. `history_entries`
13. `user_profiles`

---

## 2. 当前为什么需要把它接入核心文档

当前项目的问题不是没有表设计，而是：

1. 表设计已经存在
2. 但当前核心文档链路对它的索引仍然不够强
3. 导致后续推进容易只围绕代码现状展开，而忽略既有设计锚点

因此当前要做的不是重写表设计，而是：

`提升既有表设计在当前核心文档链路中的层级`

---

## 3. 当前应如何引用这份表设计

阶段二后续推进时，涉及以下判断必须优先回到该表设计：

1. Today 统一内容层是否实体化为 `contents`
2. 兴趣体系是否从 `users.interests` 升级到 `user_interests`
3. 设置体系是否继续沿用当前过渡方案，还是回归 `user_settings`
4. Today 快照是否进入 `briefings`
5. 报告体系是否进入 `reports`
6. 画像体系是否进入 `user_profiles`

---

## 4. 当前与代码现实的关系

### 当前代码里已经存在并可用的事实层

1. `hot_topics`
2. `opportunities`
3. `rss_articles`
4. `favorites`
5. `notes`
6. `history_entries`
7. `todos`

### 当前既有表设计里已经规划但未完全落地的结构层

1. `user_interests`
2. `user_settings`
3. `contents`
4. `briefings`
5. `reports`
6. `user_profiles`

结论：

`阶段二不是重新发明表设计，而是在既有表设计与当前代码现实之间做接轨和收束。`

---

## 5. 当前建议

后续推进顺序建议固定为：

1. 先以 [最小可用数据库表结构.sql](/E:/python/杂谈-想法记录与实践/文档/技术实现/最小可用数据库表结构.sql) 作为表设计锚点
2. 再判断当前代码已经落地到哪里
3. 再决定本轮是“继续过渡态接轨”还是“正式实体化”

当前不建议：

1. 绕开既有表设计重新起一套结构
2. 只按当前代码现状决定未来表层方向

---

## 6. 当前结论

当前阶段后续所有涉及表和结构的推进，都应明确采用以下链路：

`当前主线文档 -> 既有表设计锚点 -> 当前代码现实 -> 本轮落地动作`

这样才能保证：

1. 核心文件能索引到表设计
2. 后续实现不是另起炉灶
3. 结构收束与代码收束能对齐
