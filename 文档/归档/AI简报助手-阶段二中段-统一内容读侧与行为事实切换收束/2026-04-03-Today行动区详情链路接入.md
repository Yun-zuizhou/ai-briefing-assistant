# Today 行动区详情链路接入

- 文档状态：进行中
- 建立时间：2026-04-03
- 所属阶段：阶段二 - Today / 内容层设计与事实层收束
- 作用：记录 `worth_acting` 如何从“只有转待办动作”推进到“可读详情 + 可转待办”

---

## 1. 本轮前的实际问题

此前 Today 三个主区块里：

1. `worth_knowing` 可以点进详情
2. `recommended_for_you` 可以点进详情
3. `worth_acting` 却还停留在“只给转待办按钮”

这会导致 Today 主内容区的详情链路不一致。

---

## 2. 本轮已完成的收束

[TodayPage.tsx](/E:/python/杂谈-想法记录与实践/prototype/src/pages/TodayPage.tsx) 现已改为：

1. 整个 `worth_acting` 卡片可点击进入统一详情页
2. 卡片继续保留“转成待办”按钮
3. 按钮点击时会阻止冒泡，不会覆盖详情跳转

当前结构已经变成：

`看详情` 和 `转待办`

两条动作同时存在。

---

## 3. 当前结构判断

本轮完成后，Today 三个主内容区已全部接入统一内容详情链路：

1. `recommended_for_you`
2. `worth_knowing`
3. `worth_acting`

这意味着 Today 在“信息呈现层”的主阅读链路已经更完整。

---

## 4. 当前仍未完成

当前仍未完成：

1. 行动区从详情页直接转待办的正式联动
2. 行动区更细的状态反馈
3. 机会内容的更正式详情结构

因此当前正确表述应是：

`Today 行动区已进入“详情 + 行动”双动作过渡态，但还不是完整行动产品层`
