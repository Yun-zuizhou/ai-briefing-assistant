> 文档状态：参考文档
> 当前优先阅读：项目核心总纲 / 当前阶段总表
> 说明：本文件保留阶段十六候选主线的一轮真实联调证据与问题记录，不再作为当前阶段的第一事实源。

# 2026-04-21 阶段十六-真实联调记录

- 文档状态：阶段归档资料
- 记录日期：2026-04-21
- 文档职责：承接阶段十六首轮真实联调、截图证据、运行结果、问题清单与下一步修正入口

---

## 1. 文件定位

这份文档只承接：

1. 真实执行命令
2. 真实返回结果
3. 真实截图/证据
4. 首轮问题记录
5. 下一步修正入口

这份文档不承接：

1. 阶段十六主线定义
2. 方案设计
3. 参考案例整理

阶段主线以：

1. [2026-04-21-阶段十六-定向热点收集与AI摘要咨询执行方案.md](/E:/python/杂谈-想法记录与实践/文档/归档/AI简报助手-阶段十六-定向热点收集与AI摘要咨询执行探索/2026-04-21-阶段十六-定向热点收集与AI摘要咨询执行方案.md)

为准。

---

## 2. 本轮联调目标

本轮真实联调只验证 4 条：

1. `pipeline:collect:ai` 能否跑通
2. `pipeline:summarize:ai` 能否跑通
3. `GET /api/v1/content/daily-digest` 能否读回结果
4. `POST /api/v1/content/consult` 能否围绕单条结果回答问题

补充口径：

1. 本轮联调中的 `ai-daily` 是当前首批内部执行模板
2. 它用于技术验证，不等于产品最终对用户暴露的内容结构
3. 当前还额外补了一轮“关注 -> 内部模板”的真实验证

---

## 3. 预备条件

执行前必须确认：

1. 远端 D1 已 apply `0020_add_summary_generation_results.sql`
2. 本地 `.env` 中 D1 配置有效
3. `apps/edge-worker/.dev.vars` 中摘要 provider 配置有效
4. 测试身份 `test@example.com` 或指定用户已存在

---

## 4. 本轮执行命令

建议按固定顺序执行：

```powershell
python tools/scripts/apply_d1_remote.py
npm.cmd run pipeline:collect:ai
npm.cmd run pipeline:summarize:ai
```

然后再验证：

1. `GET /api/v1/system/summary-tasks`
2. `GET /api/v1/system/summary-tasks/:id/result`
3. `GET /api/v1/content/daily-digest?profile_id=ai-daily`
4. `POST /api/v1/content/consult`
5. 前端调试页：
   - `/ai-digest-lab`

---

## 5. 本轮真实结果记录

### 5.1 采集执行

- 执行时间：2026-04-21 12:37 左右
- 执行命令：`npm.cmd run pipeline:collect:ai`
- 结果：成功
- `ingestion_runs` 记录：已生成 `run_id = 1`
- 是否生成候选：是
- 当前结果：
  - 启用源数量：`7`
  - 原始候选：`1061`
  - 评分后候选：`24`
  - 进入摘要任务：`8`
  - 成功创建任务：`8`
- 问题：
  - `Google AI Blog` 抓取失败 1 次
  - 但不影响本轮主闭环继续推进

### 5.2 摘要执行

- 执行时间：2026-04-21 12:42 左右
- 执行命令：`npm.cmd run pipeline:summarize:ai`
- 结果：脚本执行到真实任务消费阶段，但当前 provider 配置阻塞
- `summary_generation_tasks` 记录：
  - 本轮已实际消费 `5` 条
  - 这 `5` 条当前被标记为 `failed`
  - 剩余 `3` 条仍待继续处理
- `ai_processing_runs` 记录：
  - 已写入失败执行事实
- `summary_generation_results` 记录：
  - 结果表已写入 source payload 与 task 绑定
  - 但因模型未真正返回结果，摘要正文仍未形成有效内容
- 问题：
  - 当前 `.env` 中 `AI_PROVIDER=deepseek`
  - 但对应 `DEEPSEEK_API_KEY` 实际为空
  - 导致真实请求失败并报错：`Illegal header value b'Bearer '`
  - 这说明当前阻塞点是 provider 配置，不是 D1 schema，不是 task 流转，不是执行脚本骨架

### 5.3 摘要读取

- 接口：
  - `GET /api/v1/system/summary-tasks/:id/result`
  - `GET /api/v1/content/daily-digest?profile_id=ai-daily`
- 输入：当前已具备可查询 task / result 事实
- 输出摘要标题：当前尚未形成真实摘要标题
- 输出摘要正文：当前尚未形成真实摘要正文
- 结果判断：
  - 读接口已具备
  - 当前阻塞不在读接口，而在“结果正文尚未成功生成”

补充说明：

1. 当前 `GET /api/v1/content/daily-digest` 在不传 `profile_id` 时，已默认读取当前用户全部命中模板结果
2. 这更符合项目的产品语义：用户看到的是一份围绕自己关注组织的简报，而不是单独某个内部模板的列表

### 5.4 咨询问答

- 接口：`POST /api/v1/content/consult`
- `result_ref`：接口 contract 已成立
- 问题：可提交
- 回答：当前 Workers 读侧若 provider 未配置，会按设计返回明确错误
- 证据：当前本地 `.dev.vars` 中 `SUMMARY_PROVIDER_*` 仍未补齐
- 不确定项：当前尚未完成一轮“真实成功咨询回答”
- 结果判断：
  - 咨询 contract 已成立
  - 真实成功回答仍依赖 provider 配置完成

### 5.5 关注 -> 内部模板映射

- 执行时间：2026-04-21 13:38 左右
- 执行命令：`npm.cmd run pipeline:collect:interest`
- 结果：成功
- 当前用户关注：
  - `AI`
  - `写作`
  - `D1验证20260416060644`
- 映射结果：
  - 命中模板：`ai-daily`
  - 匹配得分：`105`
  - 命中关键词：`AI`
- 未命中关注：
  - `写作`
  - `D1验证20260416060644`
- 结果判断：
  - 当前“用户关注 -> 内部模板”的最小映射骨架已真实成立
  - 当前这一轮起点仍只验证了 AI 关注
  - 后续若要承接“远程工作 / 产品设计 / 申请与论文”等关注，应继续新增对应内部模板，而不是改产品层结构

### 5.6 多命中模板验证

- 执行时间：2026-04-21 14:01 左右
- 执行命令：`python tools/scripts/run_directed_collection.py --from-user-interests --all-matched-templates`
- 结果：成功
- 当前命中模板：
  - `ai-daily`
  - `writing-opportunity-watch`
- 当前结果：
  - `ai-daily`
    - 原始候选：`1081`
    - 评分后候选：`24`
    - 跳过已存在任务：`8`
  - `writing-opportunity-watch`
    - 原始候选：`4`
    - 评分后候选：`4`
    - 当前已存在任务：`4`
- 未命中关注：
  - `D1验证20260416060644`
- 结果判断：
  - 当前系统已经不是“只有 AI 才能命中模板”
  - `写作` 这类非 AI 关注已可通过内部模板进入同一条执行链
  - 当前产品层语义仍保持不变：用户表达的是关注，系统内部才解析模板

### 5.7 远程工作模板验证

- 执行时间：2026-04-21 14:36 左右
- 执行命令：`python tools/scripts/run_directed_collection.py --user-email show@example.com --from-user-interests --all-matched-templates`
- 结果：成功
- 当前用户关注：
  - `前端`
  - `设计`
  - `远程`
- 当前命中模板：
  - `remote-opportunity-watch`
- 当前结果：
  - 原始候选：`5`
  - 评分后候选：`5`
  - 新建任务：`4`
  - 复用既有任务：`1`
- 未命中关注：
  - `前端`
  - `设计`
- 结果判断：
  - 当前 `远程` 这类非 AI、非写作关注已能进入同一条内部模板执行链
  - 当前最小映射机制已经覆盖：
    - `AI`
    - `写作`
    - `远程工作`

### 5.8 Debug Fallback 摘要执行

- 执行时间：2026-04-21 14:55 - 15:00 左右
- 执行命令：
  - `python tools/scripts/run_summary_executor.py --profile ai-daily --include-failed --debug-fallback`
  - `python tools/scripts/run_summary_executor.py --profile writing-opportunity-watch --include-failed --debug-fallback`
  - `python tools/scripts/run_summary_executor.py --profile remote-opportunity-watch --include-failed --debug-fallback`
- 结果：成功
- 当前结果层统计：
  - `ai-daily`
    - `total = 8`
    - `ready_count = 8`
  - `writing-opportunity-watch`
    - `total = 2`
    - `ready_count = 2`
  - `remote-opportunity-watch`
    - `total = 4`
    - `ready_count = 4`
- 结果判断：
  - 当前在无云 provider key 的前提下，阶段十六联调已具备一条本地可跑通的摘要结果生成路径
  - 所有 debug fallback 结果都应被视为“联调结果”，不能误写为正式模型输出

### 5.9 Cloudflare Workers AI 权限探测

- 执行时间：2026-04-21 15:00 左右
- 执行方式：
  - 先尝试 Cloudflare MCP
  - 再直接使用当前 `.env` 中的 Cloudflare 账号参数调用官方 Workers AI 接口
- 结果：失败
- 失败返回：
  - `401 Unauthorized`
  - `code = 10000`
  - `message = Authentication error`
- 结果判断：
  - 当前 `.env` 中用于 D1 的 Cloudflare token 不具备 Workers AI 调用权限
  - 因此“直接改用 Cloudflare AI 替代外部 provider key”这条路，当前阶段不应继续投入
  - 当前联调路径应继续以：
    - 外部 provider key
    - 或 debug fallback
    为准

### 5.10 NVIDIA 作为真实 Provider 的可行性

- 评估时间：2026-04-21 当前轮
- 评估结论：可行
- 当前动作：
  - 已把 `nvidia` 接为正式 provider 选项
  - 当前 `.env.example`、`ai_service.py`、`config.py`、`preflight:ai-digest` 与 `sync:ai-digest-provider` 已支持 `nvidia`
- 当前前提：
  - 仍需要真实 NVIDIA API key
  - 当前本地并未持有该 key，因此仍未进入真实调用验证
- 结果判断：
  - 若老师提供的“免费 NVIDIA 额度”最终对应可用 API key，则它可以直接替代当前空着的 DeepSeek/OpenAI 等云 provider，进入阶段十六正式模型输出路径

### 5.9 本地真实登录态接口验证

- 执行时间：2026-04-21 15:17 - 15:19 左右
- 执行环境：
  - 本地 Workers `wrangler dev`
  - 本地 Miniflare D1
  - `AI_DIGEST_DEBUG_FALLBACK=true`
  - `SUMMARY_PROVIDER_DEBUG_FALLBACK=true`
- 当前验证账号：
  - `test@example.com`
  - `show@example.com`
- 当前验证结果：
  - `test@example.com`
    - 登录：`200`
    - `GET /api/v1/content/daily-digest`：`200`
    - 返回：
      - `profileId = all`
      - `mode = all-matched-results`
      - `total = 2`
      - 第一条结果来自 `writing-opportunity-watch`
    - `POST /api/v1/content/consult`：`200`
    - 返回：
      - `providerName = debug-fallback`
      - `modelName = rule-based`
  - `show@example.com`
    - 登录：`200`
    - `GET /api/v1/content/daily-digest`：`200`
    - 返回：
      - `profileId = all`
      - `mode = all-matched-results`
      - `total = 2`
      - 第一条结果来自 `remote-opportunity-watch`
- 结果判断：
  - 当前阶段十六在本地联调口径下，已经不是“只有后台任务通”
  - 而是“真实登录态 -> digest 读取 -> consult 回答”这条接口闭环已成立

### 5.10 页面级留证

- 证据目录：
  - `test-results/ai-digest-lab/`
- 当前已落截图：
  - `test-user-lab.png`
  - `show-user-lab.png`
- 对应含义：
  - `test-user-lab.png`
    - 证明 `test@example.com` 登录后可见“AI + 写作”命中结果
  - `show-user-lab.png`
    - 证明 `show@example.com` 登录后可见“远程工作机会”命中结果
- 补充说明：
  - `consult` 的页面级单独截图本轮未稳定落盘
  - 但对应的本地真实接口调用已通过，Workers 本地日志中已可确认：
    - `POST /api/v1/content/consult -> 200`

---

## 6. 证据清单

本轮建议至少保留以下证据：

1. 采集命令执行输出
2. 摘要执行命令输出
3. `summary-tasks/:id/result` 返回截图
4. `/ai-digest-lab` 页面截图
5. 一次成功咨询返回截图或对应本地接口成功日志
6. 一次失败或异常返回截图

---

## 7. 本轮问题清单

### P0

- `.env` 中当前实际摘要 provider key 为空，导致真实摘要执行失败
- 当前已失败的 `summary task` 需要在补齐 provider 后重新执行
- 当前 `Google AI Blog` 抓取失败，需要后续判断是源失效、反爬还是 RSS 地址问题

### P1

- 当前 Workers 读侧 `consult` 变量尚未写入 `.dev.vars`
- 当前还缺一轮“成功摘要结果 + 成功咨询返回”的截图级证据
- 当前 `前端 / 设计` 这类关注还没有对应内部模板，当前会继续落入 `unmatched_interests`
- 当前 `consult` 的 debug fallback 虽已具备代码与测试，但尚未完成一轮本地实际页面联调截图

### P2

- 当前尚未评估是否需要把 `failed` task 做成显式重试 UI

---

## 8. 本轮阶段性结论

本轮联调后，只允许在以下 3 种结论中选择其一：

1. 已进入“可实践调试状态”
2. 已形成最小闭环，但仍缺真实 provider 或真实数据验证
3. 当前仍停留在接口骨架阶段，尚不能宣称链路跑通

当前选择：

- 已形成最小闭环，但仍缺真实 provider 或真实数据验证

---

## 9. 下一步入口

若回看本轮“通过后原计划”：

1. 开始做结果质量优化
2. 开始做 prompt version 管理
3. 开始做 profile 扩展

若回看本轮“未通过时原计划”：

1. 先定位 provider 配置问题
2. 再定位远端 D1 写入问题
3. 最后定位源抓取与去重问题

当前记录下来的历史下一步顺序：

1. 先补真实可用的摘要 provider key，或切到可用本地 provider
2. 若需要同步 Workers `consult` 读侧，再执行：`npm.cmd run sync:ai-digest-provider`
3. 再执行：`npm.cmd run pipeline:summarize:ai -- --include-failed`
4. 再验证：
   - `GET /api/v1/content/daily-digest?profile_id=ai-daily`
   - `POST /api/v1/content/consult`
5. 最后补成功截图与结果级文档同步
