# D1第一批行为事实表对齐

- 文档状态：进行中
- 建立时间：2026-04-04
- 所属阶段：阶段二 - Today / 内容层设计与事实层收束
- 作用：记录本轮把第一批正式数据库切换所需的 D1 结构缺口先补齐，为后续接口主读切换打底

---

## 1. 本轮目标

在已经确认“第一批正式数据库切换应先从用户行为事实层入手”之后，本轮先不切接口主读，只先补 D1 结构缺口。

本轮只做两件事：

1. 补 `user_interests`
2. 对齐 `user_settings`

---

## 2. 本轮已完成

### 2.1 已新增 D1 迁移 `0002_align_user_behavior_tables.sql`

当前在：

- [0002_align_user_behavior_tables.sql](/E:/python/杂谈-想法记录与实践/cloudflare/d1/migrations/0002_align_user_behavior_tables.sql)

已完成：

1. 新增 `user_interests`
2. 为 `user_settings` 补上：
   - `morning_brief_time`
   - `evening_brief_time`
   - `do_not_disturb_enabled`
   - `sound_enabled`
   - `vibration_enabled`
3. 同时把旧的：
   - `morning_briefing_time`
   - `evening_briefing_time`

向当前本地真实表字段口径回填对齐

这意味着当前 D1 结构已经开始从“旧稿”转向“当前本地真实行为事实层口径”。

### 2.2 已补 D1 远端种子生成脚本对齐

当前在：

- [generate_d1_remote_seed.py](/E:/python/杂谈-想法记录与实践/scripts/generate_d1_remote_seed.py)

已完成：

1. 开始导出 `user_interests`
2. `user_settings` 改按当前真实字段生成：
   - `morning_brief_time`
   - `evening_brief_time`
   - `do_not_disturb_enabled`
   - `sound_enabled`
   - `vibration_enabled`

### 2.3 D1 准备链路已重新验证

本轮已重新执行：

- `node scripts/prepare-d1.mjs`

当前结果：

1. `seed.remote.sql` 已重新生成
2. 其中已能看到：
   - `INSERT OR REPLACE INTO user_interests`
   - 新版 `user_settings` 字段口径
3. 当前机器仍未安装 `wrangler`
4. 因此这一步当前只完成到“本地准备完成，远端尚未真正 apply”

---

## 3. 本轮当前正确口径

当前应更新为：

1. 第一批正式数据库切换还没有开始接口主读切换
2. 但 D1 结构缺口已经开始补齐
3. 当前最关键的两个前置缺口：
   - `user_interests`
   - `user_settings`
   已经不再只是设计判断，而有了真实 migration 与 seed 对齐

---

## 4. 下一步建议

顺着当前主线，下一步最合理的是：

1. 继续补第一批接口的 D1 访问层方案
2. 优先考虑：
   - `preferences/interests`
   - `preferences/settings`
3. 再决定 `todos / notes / favorites / history / chat` 如何进入正式数据库主读切换
