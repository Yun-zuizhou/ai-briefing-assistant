# D1远端apply前置阻塞说明

- 文档状态：进行中
- 建立时间：2026-04-04
- 所属阶段：阶段二 - Today / 内容层设计与事实层收束
- 作用：记录本轮在完成 D1 migration、种子脚本和远端 apply 入口后，当前剩余的唯一真实阻塞

---

## 1. 本轮已完成

本轮已经完成：

1. D1 第二条 migration 已补
2. D1 远端种子脚本已对齐
3. 远端 apply 入口已补：
   - `python scripts/apply_d1_remote.py`
   - `npm.cmd run db:d1:apply`
   - `npm.cmd run db:d1:apply-seed`
4. `app.services` 包初始化的副作用问题已收掉

这意味着当前不能继续推进的原因，已经不再是本地代码问题。

---

## 2. 原阻塞项

当前实际执行：

- `python scripts/apply_d1_remote.py`

返回的真实错误是：

- `D1 配置缺失：D1_ACCOUNT_ID, D1_API_TOKEN`

当时的唯一阻塞是：

1. `.env` 里没有填 `D1_ACCOUNT_ID`
2. `.env` 里没有填 `D1_API_TOKEN`

---

## 3. 当前更新口径

当前已更新为：

1. 稳定凭证已完成落地，`D1_ACCOUNT_ID / D1_DATABASE_ID / D1_API_TOKEN` 已进入本地配置
2. Cloudflare D1 远端 migration apply 已完成
3. `D1_USE_CLOUD_AS_SOURCE=true` 已开启，第一批行为事实接口云端主读专项验证已通过
4. 当前剩余问题已不再是凭证阻塞，而是 `seed.remote.sql` 整包远端 apply 会遇到网络超时

---

## 4. 当前下一步

当前下一步应转为：

1. 处理 `seed.remote.sql` 的远端超时问题
2. 评估是否拆包 seed 或分批灌装
3. 再推进 `Today / content / reports` 这类聚合读侧的正式数据库切换
