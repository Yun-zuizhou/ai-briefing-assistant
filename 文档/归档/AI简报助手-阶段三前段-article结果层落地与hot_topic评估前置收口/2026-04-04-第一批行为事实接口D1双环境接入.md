# 第一批行为事实接口D1双环境接入

- 文档状态：进行中
- 建立时间：2026-04-04
- 所属阶段：阶段二 - Today / 内容层设计与事实层收束
- 作用：记录本轮把第一批行为事实接口从“只有本地 SQLite 主读”推进到“Cloudflare D1 云端主读已成立”的完整任务包进展

---

## 1. 本轮目标

本轮不再拆成两个接口一个接口推进，而是直接执行：

`第一批行为事实接口正式数据库接入包`

本轮覆盖范围：

1. `preferences/interests`
2. `preferences/settings`
3. `preferences/profile`
4. `todos`
5. `notes`
6. `favorites`
7. `history`
8. `chat/execute`
9. `chat/reclassify`

---

## 2. 本轮已完成

### 2.1 已新增统一 D1 行为事实存储层

当前在：

- [d1_client.py](/E:/python/杂谈-想法记录与实践/app/services/d1_client.py)
- [d1_behavior_store.py](/E:/python/杂谈-想法记录与实践/app/services/d1_behavior_store.py)

已完成：

1. D1 基础请求封装
2. 第一批行为事实表对应的读写方法
3. Chat 执行 / 纠偏所需的最小行为事实方法

这意味着当前不是每个接口各自写一套 D1 逻辑，而是已经开始共享同一层行为事实访问层。

### 2.2 第一批接口已具备 D1 双环境访问分支

当前这些接口都已补：

1. `preferences/interests`
2. `preferences/settings`
3. `preferences/profile`
4. `todos`
5. `notes`
6. `favorites`
7. `history`
8. `chat/execute`
9. `chat/reclassify`

当前行为是：

1. `D1_USE_CLOUD_AS_SOURCE=false`
   - 继续读本地 SQLite
2. `D1_USE_CLOUD_AS_SOURCE=true`
   - 切到 D1 行为事实存储层

### 2.3 当前这一步的真实定位

当前要明确：

1. 这不等于“正式数据库已经全量切完”
2. 但它已经不再只是 migration、设计稿或双环境访问层准备
3. 而是第一批接口已经真正进入云端正式数据库主读验证通过状态

也就是说，本轮之后，当前数据库主线已经从：

- 只在结构准备层 / 双环境访问层

推进到：

- 第一批行为事实接口已具备稳定凭证、已完成远端 migration apply，并已通过云端主读专项验证

### 2.4 本轮新增落地结果

本轮继续完成了以下关键动作：

1. 已补稳定凭证落地脚本：
   - [bootstrap_d1_credentials.py](/E:/python/杂谈-想法记录与实践/scripts/bootstrap_d1_credentials.py)
2. `D1_ACCOUNT_ID / D1_DATABASE_ID` 已可自动从本机 Cloudflare 元数据与 `wrangler.toml` 发现
3. `D1_API_TOKEN` 已完成一次性落地，本轮不再需要重复手填账号与数据库 ID
4. Cloudflare D1 远端 migration apply 已完成：
   - `0001_init_schema.sql`
   - `0002_align_user_behavior_tables.sql`
5. 当前配置已切到：
   - `D1_USE_CLOUD_AS_SOURCE=true`
6. 已新增云端主线专项验证脚本：
   - [verify_d1_behavior_mainline.py](/E:/python/杂谈-想法记录与实践/scripts/verify_d1_behavior_mainline.py)
7. 已修复两处 D1 分支暴露的真实代码问题：
   - [chat.py](/E:/python/杂谈-想法记录与实践/app/api/v1/chat.py) 缺少 `D1BehaviorStore` 导入
   - [preferences.py](/E:/python/杂谈-想法记录与实践/app/api/v1/preferences.py) 的局部变量 `settings` 遮蔽了全局配置对象

---

## 3. 本轮实际验收

本轮已通过：

1. `npm.cmd run test:api-mainline`
2. 默认 `pytest`
3. 根目录 `npm.cmd run build`
4. 根目录 `npm.cmd run lint`
5. `python scripts/apply_d1_remote.py`
6. `python scripts/verify_d1_behavior_mainline.py`

当前结果：

1. `npm.cmd run test:api-mainline` 为 `16 passed`
2. 默认 `pytest` 为 `31 passed, 1 skipped`
3. 根目录前端 `build` 通过
4. 根目录前端 `lint` 通过
5. D1 远端 migration apply 成功
6. 第一批行为事实接口云端主读专项验证通过

补充说明：

1. `npm.cmd run test:api-mainline` 当前已固定回本地 SQLite 契约测试口径，避免被云端主读开关污染
2. 云端正式数据库主线验证已单独拆到：
   - `python scripts/verify_d1_behavior_mainline.py`
3. 这意味着当前“本地契约验收”和“云端主读验收”已经分口径管理

---

## 4. 当前正确口径

当前应更新为：

1. 第一批行为事实接口已经完成 Cloudflare D1 正式数据库切换验证
2. 当前运行配置下，这批接口已经按 `D1_USE_CLOUD_AS_SOURCE=true` 走云端主读
3. 本地 SQLite 不再是这批接口的默认主读，但仍保留为本地运行与未切接口的事实来源之一
4. 当前“正式切到云端数据库”的成立范围，只覆盖：
   - `preferences/interests`
   - `preferences/settings`
   - `preferences/profile`
   - `todos`
   - `notes`
   - `favorites`
   - `history`
   - `chat/execute`
   - `chat/reclassify`
5. 当前不能扩张解释为：
   - `Today / content / reports` 也已经完成云端正式切换
6. 当前剩余问题已从“凭证阻塞 / apply 阻塞”收敛为：
   - `seed.remote.sql` 整包远端 apply 会遇到网络超时
   - 聚合读侧仍未进入正式数据库切换

---

## 5. 下一步建议

顺着当前主线，下一步最合理的是：

1. 处理 `seed.remote.sql` 远端 apply 超时，决定是否拆包种子或分批灌装
2. 保持第一批行为事实接口在 `D1_USE_CLOUD_AS_SOURCE=true` 下继续观察稳定性
3. 在此基础上，再推进 `Today / content / reports` 这类聚合读侧的正式数据库切换
