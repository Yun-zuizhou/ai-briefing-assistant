# 2026-04-16 D1主链路闭环修复与验收

- 记录日期：2026-04-16
- 记录类型：进行中主线执行记录
- 目标：围绕“对话识别关注词 -> 写入关注清单 -> 推荐读取关注词”打通后端主链路，并优先保障 D1 主读场景可验证。

## 1. 本轮执行范围

1. 修复 `chat/reclassify` 在 D1 分支的会话消息持久化缺陷。
2. 修复本地主链 `actions/check-in` 的 streak 计数回归问题。
3. 针对目标场景补一条“关注 AI 闭环”自动化回归测试。
4. 补强 D1 行为主链验证脚本中的“关注 AI 闭环”验收步骤。

## 2. 本轮代码变更

1. `app/api/v1/chat.py`
   - 修复 D1 reclassify 分支 `session_store` 未定义问题。
   - 清理 execute 分支重复初始化 `session_store` 的冗余代码。
2. `app/api/v1/actions.py`
   - 本地 `check-in` 写入 `HistoryEntry` 时显式设置 `created_at=datetime.now()`，避免本地时区边界导致 streak=0。
3. `app/api/v1/intent.py`
   - 移除待办动作词中的单字 `得`，避免“得到更多咨询”误判为待办。
   - 调整否定前缀识别逻辑：从“任意包含否定字”改为“句首否定 + 明确否定短语”，避免“特别关注”被误判为否定。
4. `workers/src/routes/chat.ts`
   - 同步否定前缀识别逻辑，避免 Python/Workers 识别规则漂移。
5. `tests/test_api_mainline.py`
   - 新增 `test_chat_execute_interest_flow_updates_dashboard_recommendations`，覆盖场景：
     - 对话输入“今天特别关注了AI，以后希望得到更多关于AI的咨询”
     - `chat/execute` 识别为 `add_interest`
     - `preferences/interests` 包含 `AI`
     - `dashboard/today` 推荐块出现 `AI` 关注项
6. `scripts/verify_d1_behavior_mainline.py`
   - 新增云端 D1 闭环步骤：chat 增加 AI 关注 -> interests 读回 -> today 推荐命中 AI。
   - 调整周报与报告列表校验顺序：先生成周报，再校验 `/reports` 可见性。

## 3. 验证结果

1. `pytest tests/test_api_mainline.py::test_actions_check_in_creates_real_backend_state -q`
   - 通过。
2. `pytest tests/test_api_mainline.py::test_chat_execute_interest_flow_updates_dashboard_recommendations -q`
   - 通过。
3. `pytest tests/test_api_mainline.py -q`
   - 通过（`28 passed`）。
4. `python -m py_compile app/api/v1/intent.py app/api/v1/chat.py app/api/v1/actions.py scripts/verify_d1_behavior_mainline.py`
   - 通过。
5. `npm.cmd run typecheck`（`workers/`）
   - 通过。
6. `python scripts/verify_d1_behavior_mainline.py`
   - 通过（已包含“关注 AI 闭环”断言）。
7. `npm.cmd run check`
   - 通过（`75 passed`）。

## 4. 当前阻塞与事实

1. `npm.cmd run check` 已恢复全绿，当前无门槛级阻塞。
2. 已在 D1 验证脚本中新增“凭据前置探测”（`SELECT 1 AS ok`），用于防止本地回退掩盖云端鉴权问题。
3. 中断恢复后补充修复：
   - `prototype/src/pages/ArticlePage.tsx`：补齐契约要求文案“机会型内容，已支持从详情页继续进入对话页”。
   - `文档/项目核心总纲.md`：补齐“Cloudflare D1 真实接管”口径，确保契约测试与核心文档一致。

## 5. 下一步入口

1. 在页面级人工验收中补一次“对话->关注AI->推荐命中AI”的云端截图级验收记录。
2. 继续补齐主链之外的次链路（机会跟进结果回收、推送调度执行链）并纳入自动化验收。

## 6. 本轮追加修复（D1 严格真实链）

1. `workers/src/routes/chat.ts`
   - 去掉 `execute/reclassify` 的“虚拟成功回退”。
   - 数据库写入异常时改为显式失败返回（HTTP 500），避免“看起来成功、实际未入库”。
2. `workers/src/services/contentStore.ts`
   - `opportunities` 与 `user_interests` 的状态过滤改为 `lower(status)='active'`，兼容历史大小写混用数据。
3. `workers/src/routes/actions.ts`
   - `briefing_schedules` 状态过滤改为 `lower(status)='active'`，避免调度事实层回显漏读。
4. `app/services/d1_content_store.py`
   - D1 机会读取从 `status='ACTIVE'` 改为 `lower(status)='active'`，与 Workers 语义对齐。
5. `app/services/d1_behavior_store.py`
   - D1 兴趣读取从 `status='active'` 改为 `lower(status)='active'`，兼容历史数据。

### 6.1 本轮复验结果

1. `npm.cmd run typecheck`（`workers/`）通过。
2. `npm.cmd run check` 通过（`75 passed`）。
3. `python scripts/verify_d1_behavior_mainline.py` 通过（Cloud D1 主链通过）。
