# 2026-04-16 仓库结构收口与接口契约归位

- 记录日期：2026-04-16
- 记录类型：进行中主线执行记录
- 目标：在不破坏现有链路与门槛的前提下，完成运行层目录归位、共享契约抽取、D1 迁移/seed 目录重整，并清理根目录历史占位入口。

## 1. 三轮“如果不这样做还有什么方法”

### 第一轮（是否只写规划，不动目录）

1. 方案 A：只输出重组清单，不改仓库。
2. 方案 B：只改脚本入口，不改目录归位。
3. 方案 C：直接完成正式运行层目录归位，并同步修脚本/测试/配置。

结论：选 C。当前 `prototype / workers / scripts / cloudflare` 的旧路径已经进入脚本、测试、配置和认知层，不真正落地迁移，后续会持续回退到旧结构。

### 第二轮（谁是正式在线后端）

1. 方案 A：把 FastAPI 重新抬回主后端。
2. 方案 B：保留双主后端并列扩展。
3. 方案 C：Workers 继续作为正式在线 API 主线，FastAPI 保留为离线/参考/验证服务。

结论：选 C。当前线上与阶段文档都已经指向 `Workers + Pages + D1`，这次结构收口不能再把接口主线重新分裂。

### 第三轮（共享契约如何落）

1. 方案 A：继续分别维护 `apps/web` 与 `apps/edge-worker` 的 `page-data.ts`。
2. 方案 B：只抽文档契约，不抽代码契约。
3. 方案 C：抽出 `packages/contracts/src/page-data.ts` 作为单一源文件，两侧本地类型文件只做 re-export。

结论：选 C。接口稳定已经是当前主线要求，继续双拷贝维护字段只会让页面、Workers 和 Python 契约测试再次漂移。

## 2. 本轮最终决策

采用“运行层先归位、接口契约单点化、D1 目录跟随迁移”的一次性收口策略：

1. 正式前端迁入 `apps/web`
2. 正式在线后端迁入 `apps/edge-worker`
3. 工具脚本迁入 `tools/scripts`
4. D1 迁移与 seed 迁入 `infra/cloudflare/d1`
5. 前后端共享契约迁入 `packages/contracts`
6. 本地 SQLite / 日志迁入 `var/local` 与 `var/logs`
7. 根目录历史 Cloudflare 占位入口归档，不再保留第二套 Worker 入口

## 3. 本轮代码与结构变更

### 3.1 目录归位

1. `prototype -> apps/web`
2. `workers -> apps/edge-worker`
3. `scripts -> tools/scripts`
4. `cloudflare/d1 -> infra/cloudflare/d1`
5. `info_collector.db -> var/local/info_collector.db`
6. `debug.log -> var/logs/debug.log`
7. 根目录 `wrangler.toml` / `worker.js` 已归档到 `_archive_workspace/legacy-root-cloudflare/`
8. `railway.toml` / `Procfile` / `runtime.txt` 已迁入 `infra/deploy/`

### 3.2 接口契约收口

1. 新增共享契约文件：
   - [packages/contracts/src/page-data.ts](/E:/python/杂谈-想法记录与实践/packages/contracts/src/page-data.ts)
2. `apps/web` 与 `apps/edge-worker` 的本地 `page-data.ts` 已改为薄 re-export，避免重复维护字段。

### 3.3 脚本与运行链收口

1. 根 `package.json` 已统一改到 `tools/scripts/*`
2. `tools/scripts/_shared.mjs` 已收口目录常量：
   - `apps/web`
   - `apps/edge-worker`
   - `infra/cloudflare/d1`
3. `check / setup / dev / test:workers / preflight:workers-sync / db:d1:prepare` 均已改到新路径
4. D1 客户端读取的正式 wrangler 配置已切到 `apps/edge-worker/wrangler.toml`

### 3.4 D1 目录与数据资产收口

1. 迁移文件目录：`infra/cloudflare/d1/migrations`
2. 生成 seed 目录：`infra/cloudflare/d1/seeds/generated`
3. 预留手写 seed 目录：`infra/cloudflare/d1/seeds/source`
4. 生成脚本、拆分脚本、远端 apply 脚本均已切到新目录

## 4. 验证结果

1. `python -m pytest -q tests/test_dashboard_today.py tests/test_today_mainline_smoke.py`
   - 通过（`27 passed`）
2. `npm.cmd run check:workers`
   - 通过（`12 files / 33 tests`）
3. `npm.cmd run check`
   - 通过（`75 passed`）
4. `npm.cmd run db:d1:prepare`
   - 通过，生成物已落到 `infra/cloudflare/d1/seeds/generated/`

## 5. 当前边界与刻意未动的部分

1. `app/` 本轮未迁入 `apps/api`，继续保留在根目录：
   - 原因：当前 Python 仍承担离线/参考/验证职责，直接移动会放大 `PYTHONPATH`、测试入口和文档链路风险。
2. `文档/` 与 `_archive_workspace/` 本轮未改名：
   - 原因：当前协作规范、契约测试与大量历史文档都强绑定这两条路径，应单开一轮文档治理主线处理。
3. 这次是“运行层与接口层先收口”，不是“全仓库一次性改名”。

## 6. 纠正机制

1. 后续只允许 `packages/contracts/src/page-data.ts` 作为共享页面契约单一源。
2. 新增 D1 seed 产物只允许写到 `infra/cloudflare/d1/seeds/generated`。
3. 若脚本再次引用 `prototype / workers / scripts / cloudflare/d1` 旧路径，视为结构回退，必须立即修正。
4. 根目录不再新增第二套 Worker 入口；Cloudflare 正式入口只认 `apps/edge-worker/wrangler.toml`。

## 7. 下一步入口

1. 若继续推进仓库结构治理，下一优先是评估 `app -> apps/api` 的安全迁移条件。
2. 若继续推进链路稳定化，下一优先是补 `check:stage` 与 `preflight:workers-sync` 在新目录结构下的持续回归证据。
3. 若继续推进文档治理，下一优先是把核心文档里的正式目录口径统一改到 `apps / tools / infra / packages / var`。
