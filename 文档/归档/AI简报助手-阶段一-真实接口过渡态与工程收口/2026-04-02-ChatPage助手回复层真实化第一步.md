# 2026-04-02-ChatPage助手回复层真实化第一步

- 文档状态：进行中
- 所属阶段：对话页真实接口过渡态收束
- 记录时间：2026-04-02

---

## 1. 本次目标

在已完成：

1. `ChatPage` 真实执行优先
2. `ChatPage` 消息层收口第一步

之后，继续推进“助手回复层真实化”。

本次目标不是建设完整 AI 对话生成系统，而是先让当前助手消息正文从：

`前端固定模板 generateResponse() 主导`

推进为：

`真实 execute 结果主导 + 前端模板兜底`

---

## 2. 本次完成内容

处理文件：

- `prototype/src/hooks/useChatLogic.ts`

### 2.1 新增真实执行结果到助手回复正文的组装逻辑

本次新增：

- `buildAssistantReplyFromActionSummary()`

当前组装优先使用：

1. `successMessage`
2. `resultSummary`
3. `nextPageLabel`
4. `quickActions`

### 2.2 sendMessage() 逻辑调整

当前对话发送流程调整为：

1. 先识别意图
2. 优先调用 `/api/v1/chat/execute`
3. 如果成功拿到真实执行结果：
   - 执行反馈区使用真实结果
   - 助手消息正文也优先基于真实结果组装
4. 如果真实执行结果不可用：
   - 再回退到本地动作摘要
   - 再必要时回退到 `generateResponse()`

---

## 3. 当前状态判断

### 已进入真实接口过渡态

1. 执行反馈区
   - 已优先绑定真实 `chat/execute`

2. 助手消息正文
   - 已优先绑定真实 `successMessage / resultSummary / nextPageLabel / quickActions`

3. 本地模板回复
   - 当前已降为兜底层

### 仍然属于过渡态

1. 助手回复不是完整后端生成
   - 当前只是基于真实执行结果做前端组装
   - 不是完整后端对话内容生成系统

2. 多轮上下文对话
   - 仍未建立真正的上下文记忆和消息持久化

3. 自然语言丰富度
   - 当前回复更偏执行反馈，不是完整自然对话体验

---

## 4. 阶段判断更新

本次完成后，可以更准确地说：

1. `ChatPage` 当前不只是“真实执行，假回复”
2. `ChatPage` 已进入“真实执行结果主导的回复层过渡态”
3. 当前剩余问题继续集中在：
   - 消息是否真正持久化
   - 回复是否需要完整后端生成
   - 对话上下文是否需要系统化

---

## 5. 验证结果

已完成：

1. 前端构建通过
2. 后端语法检查通过

结果：

- `npm.cmd run build` 通过
- `chat backend compile ok`

---

## 6. 下一步建议

当前更合理的下一步是：

1. 判断是否继续做 `ChatPage` 的消息持久化
2. 继续压缩 `AppContext` 在对话链路中的残余职责
3. 暂时不进入完整大模型对话生成设计，先把执行型对话链路彻底收口
