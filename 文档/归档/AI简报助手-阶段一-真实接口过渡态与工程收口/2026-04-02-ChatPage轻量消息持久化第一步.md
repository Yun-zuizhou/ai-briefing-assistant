# 2026-04-02-ChatPage轻量消息持久化第一步

- 文档状态：进行中
- 所属阶段：对话页真实接口过渡态收束
- 记录时间：2026-04-02

---

## 1. 本次目标

按“轻量版消息持久化，但为正式版预留升级空间”的原则，先让 `ChatPage` 的消息：

1. 可保存
2. 可恢复
3. 结构上可向正式版演进

本次不做完整后端会话系统，不新增正式聊天表。

---

## 2. 本次完成内容

处理文件：

- `prototype/src/hooks/useChatLogic.ts`

### 2.1 新增轻量持久化存储

当前已新增：

- `CHAT_STORAGE_KEY = 'jianbao_chat_state_v1'`
- `loadPersistedChatState()`
- `persistChatState()`

当前 `ChatPage` 消息会写入本地存储，并在重新进入页面时恢复。

### 2.2 消息结构按正式版骨架预留升级字段

当前本地持久化消息结构已包含：

- `sessionId`
- `id`
- `role`
- `content`
- `createdAt`
- `intentType`
- `actionType`
- `resultSummary`
- `deepLink`

这意味着：

- 虽然当前还只是本地轻量持久化
- 但结构上已经保留了未来正式版 `chat_sessions / chat_messages` 所需要的核心方向

### 2.3 当前消息主写入路径已统一

当前对话消息写入路径为：

1. 用户消息写入 `useChatLogic` 局部状态
2. 同时落到本地持久层
3. 助手消息写入 `useChatLogic` 局部状态
4. 同时落到本地持久层

---

## 3. 当前状态判断

### 已进入真实接口过渡态

1. 执行层
   - 仍优先走 `/api/v1/chat/execute`

2. 消息层
   - 已具备轻量持久化
   - 刷新或重新进入后可恢复

3. 结构层
   - 已预留正式版升级字段

### 仍然属于过渡态

1. 消息存储位置
   - 当前仍是前端本地存储
   - 不是后端正式持久化

2. 会话管理
   - 当前只有最小 `sessionId`
   - 还没有完整会话列表、标题、归档机制

3. 多端一致性
   - 当前不存在
   - 仅限当前浏览器本地

---

## 4. 阶段判断更新

本次完成后，可以更准确地说：

1. `ChatPage` 已不再只是“页面内临时消息流”
2. `ChatPage` 已进入“轻量消息持久化过渡态”
3. 当前离正式版的主要差距不在消息结构，而在：
   - 后端正式落库
   - 会话体系
   - 多端一致性

---

## 5. 验证结果

已完成：

1. 前端构建通过

结果：

- `npm.cmd run build` 通过

---

## 6. 下一步建议

当前更合理的下一步是：

1. 先不急着上完整正式版聊天表
2. 继续压缩 `AppContext` 在对话链路中的残余职责
3. 等 Today / 内容层 / Chat 统一边界更稳定后，再决定是否进入正式版 `chat_sessions / chat_messages`
