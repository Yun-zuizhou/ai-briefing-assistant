# 2026-04-02-第二批第三阶段-Reports真实接口过渡态

- 文档状态：进行中
- 所属阶段：页面真实接口过渡态收束
- 记录时间：2026-04-02

---

## 1. 本次目标

将 `WeeklyReportPage / MonthlyReportPage / AnnualReportPage` 从前端 `mockData / 硬编码` 主驱动切到真实接口过渡态。

本次目标不是直接建设完整正式报告系统，而是先保证：

1. 报告页不再把前端假数据当主事实源
2. 报告页至少进入真实接口聚合过渡态
3. 真实来源明确来自现有事实层

---

## 2. 本次完成内容

### 2.1 后端新增独立 reports 聚合接口

新增：

- `GET /api/v1/reports`
- `GET /api/v1/reports/weekly`
- `GET /api/v1/reports/monthly`
- `GET /api/v1/reports/annual`

涉及文件：

- `app/api/v1/reports.py`
- `app/api/v1/page_schemas.py`
- `app/api/v1/__init__.py`
- `app/main.py`

### 2.2 前端补报告类型与 API 调用

涉及文件：

- `prototype/src/types/page-data.ts`
- `prototype/src/services/api.ts`

新增内容：

- `PeriodicReportData`
- `AnnualReportData`
- `apiService.getReports()`
- `apiService.getWeeklyReport()`
- `apiService.getMonthlyReport()`
- `apiService.getAnnualReport()`

### 2.3 三个报告页切到真实接口过渡态

涉及文件：

- `prototype/src/pages/WeeklyReportPage.tsx`
- `prototype/src/pages/MonthlyReportPage.tsx`
- `prototype/src/pages/AnnualReportPage.tsx`

当前状态：

- 周报页：已接入 `/api/v1/reports/weekly`
- 月报页：已接入 `/api/v1/reports/monthly`
- 年报页：已接入 `/api/v1/reports/annual`

---

## 3. 当前真实数据来源

当前报告接口聚合使用的真实来源为：

- `users.interests`
- `notes`
- `favorites`
- `todos`
- `history_entries`

说明：

1. 这些来源属于当前项目已经存在的最小事实层或真实接口过渡层
2. 报告页现在展示的数据，不再主要来自前端 `mockData`

---

## 4. 当前仍然属于过渡态的部分

以下内容仍不是正式态：

1. 报告文案解释
   - 当前为接口内基于真实数据聚合生成的过渡态文案
   - 还不是正式报告事实表或正式内容生产链路

2. 报告趋势解释
   - 当前是按真实记录、收藏、历史、兴趣生成的最小可读趋势
   - 还不是统一内容层或真实热点分析引擎输出

3. 年度分享图片与正式导出能力
   - 目前仍未正式实现
   - 只保留提示和最小导出能力

4. `reports` 正式事实层
   - 当前未新建正式 `reports` 持久化表
   - 本次采用的是“真实接口聚合过渡态”，不是最终正式态

---

## 5. 验证结果

已完成验证：

1. 后端语法检查通过
2. 前端构建通过

执行结果：

- `py_compile ok`
- `npm.cmd run build` 通过

当前非阻塞项：

- Vite 仍提示 chunk 体积偏大
- 该问题目前属于性能优化项，不影响本次构建收口

---

## 6. 阶段判断更新

本次完成后，可以明确认为：

1. `Reports` 已不再属于纯前端 mock 页面
2. `Reports` 已进入真实接口过渡态
3. 项目当前剩余最重问题继续集中在：
   - `TodayPage`
   - `ChatPage`
   - 统一内容供给层

---

## 7. 下一步建议

当前更合理的下一步是：

1. 核对 `ArticlePage / ArticleReaderPage` 是否需要同步进入真实接口过渡态
2. 继续收束 `TodayPage` 的内容获取、加工、整合、呈现四层链路
3. 最后集中统一 `ChatPage` 与真实事实层、真实内容层的边界
