# 2026-04-02-ChatPage消息层收口第一步

- 文档状态：进行中
- 所属阶段：对话页真实接口过渡态收束
- 记录时间：2026-04-02

---

## 1. 本次目标

在已完成“真实执行优先”之后，继续收口 `ChatPage` 的消息层。

本次目标不是建设完整对话持久化系统，而是先让当前对话主流程不再继续依赖：

`AppContext.messages`

作为主消息源。

---

## 2. 本次完成内容

处理文件：

- `prototype/src/hooks/useChatLogic.ts`
- `prototype/src/pages/ChatPage.tsx`

### 2.1 消息流主状态收回到 useChatLogic

本次调整后：

1. `useChatLogic` 内部新增局部 `messages` 状态
2. `sendMessage()` 产生的用户消息与助手消息，改为写入该局部状态
3. `ChatPage` 当前优先读取 `useChatLogic` 返回的 `messages`

这意味着：

- 当前对话页消息流已经不再由全局 `AppContext.messages` 主导

### 2.2 ChatPage 解除对全局消息态的直接依赖

本次调整后：

- `ChatPage` 已移除对 `useAppContext().messages` 的直接读取
- 当前页面消息展示改由 `useChatLogic()` 提供

---

## 3. 当前状态判断

### 已进入真实接口过渡态

1. 执行链路
   - 仍优先走 `/api/v1/chat/execute`

2. 消息流主状态
   - 当前已从全局 `AppContext.messages` 收回到 `useChatLogic` 局部状态

3. 页面展示
   - 当前对话页展示的消息流已不再依赖全局演示聊天历史

### 仍保留的过渡兼容态

1. `AppContext.messages`
   - 仍然存在于上下文结构中
   - 但当前已不再作为 `ChatPage` 主链消息源

2. 助手回复文案
   - 当前仍主要由前端 `generateResponse()` 生成

3. 对话持久化
   - 当前消息流仍不是后端持久化对话记录

---

## 4. 阶段判断更新

本次完成后，可以更准确地说：

1. `ChatPage` 当前已不再是“全局演示聊天历史驱动页”
2. `ChatPage` 已进入“真实执行优先 + 局部消息流主导”的过渡态
3. 当前剩余问题继续集中在：
   - 助手回复层是否真实化
   - 对话消息是否需要真正持久化
   - `AppContext` 中旧消息状态何时彻底退场

---

## 5. 验证结果

已完成：

1. 前端构建通过

结果：

- `npm.cmd run build` 通过

---

## 6. 下一步建议

当前更合理的下一步是：

1. 继续决定是否让助手回复文案优先绑定真实执行结果，而不是前端固定模板
2. 评估 `AppContext.messages` 是否可以逐步退出上下文主结构
3. 如果继续深收口，再决定是否做真正的对话消息持久化
