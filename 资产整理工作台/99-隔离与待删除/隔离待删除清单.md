# 隔离与待删除清单

- 建立日期：2026-04-28
- 作用：记录不应进入正式主线、后续可清理或只保留本地环境意义的资产。

## 隔离资产

| 资产 | 路径 | 当前判断 | 后续动作 |
|---|---|---|---|
| Node 依赖 | `node_modules` | 可重建依赖目录 | 不迁移，不纳入资产治理 |
| Python 本地依赖 | `.pydeps` / `.pydeps_runtime` | 本地依赖缓存 | 不迁移，只保留环境规则 |
| 测试临时目录 | `.pytest_tmp` | 运行产物 | 后续可清理 |
| 运行日志 | `var/logs` / `var/*.log` | 运行产物 | 统一日志路径和 ignore |
| 本地数据库 | `var/local` | 本地过渡数据 | 不作为正式云端事实源 |
| IDE / 笔记配置 | `.obsidian` / `.trae` / `.config` | 本地工作环境 | 不进软件资产主线 |
| Wrangler 本地缓存 | `.wrangler` / `.wrangler-logs` | 本地运行缓存 | 不迁移 |

## 待确认旧结构

| 旧结构 | 当前 Git 表现 | 目标判断 |
|---|---|---|
| `prototype` | tracked 删除项 | 确认是否已迁入 `apps/web` / demo / 归档 |
| `scripts` | tracked 删除项 | 确认是否已迁入 `tools/scripts` |
| `cloudflare/d1` | tracked 删除项 | 确认是否已迁入 `infra/cloudflare/d1` |
| 根级 `wrangler.toml` / `worker.js` | tracked 删除项 | 确认已退出正式入口 |
| Railway 文件 | tracked 删除项 | 确认已退出当前部署主线 |

## 删除前规则

1. 不删除当前 Git 中未确认来源的旧结构。
2. 不删除任何仍被 `package.json`、脚本、测试或文档事实源引用的文件。
3. 删除前先在 `迁移决策记录.md` 写明理由。
4. 删除后必须跑对应验证入口，至少确认 `npm.cmd run check` 不受影响。
