# 迁移决策记录

- 建立日期：2026-04-28
- 作用：记录每一次资产整理中的保留、迁移、归档、隔离、删除判断。

## 决策模板

```md
## YYYY-MM-DD 决策标题

- 涉及资产：
- 当前路径：
- 目标分区：
- 决策：
- 理由：
- 风险：
- 后续动作：
```

## 已记录决策

## 2026-04-28 建立资产整理工作台

- 涉及资产：全仓库资产治理
- 当前路径：仓库根目录
- 目标分区：`资产整理工作台`
- 决策：先建立清单工作台，不移动现有代码、数据、脚本和文档主线。
- 理由：当前仓库仍需要保持可运行状态；直接搬迁会增加运行入口、测试入口和 Git 状态风险。
- 风险：如果后续只建清单不执行瘦身，工作台会变成新的文档负担。
- 后续动作：按 `待整理清单.md` 分批核查旧结构、文档事实源、UI 资产、数据库生成物和运行产物。

## 2026-04-28 正式主线暂不搬动

- 涉及资产：`apps/web`、`apps/edge-worker`、`packages/contracts`、`infra/cloudflare/d1`、`tools/scripts`
- 当前路径：仓库现有正式目录
- 目标分区：资产清单中的正式运行主线、后端稳定区、数据库与运行规范区
- 决策：不复制、不移动这些目录，只在资产清单中登记身份。
- 理由：这些目录已经被 README、技术现实清单、命令规范和测试门槛引用，直接搬动会破坏当前运行链。
- 风险：目录视觉上暂时不会变得更“干净”。
- 后续动作：先完成 Git 基线确认，再决定是否做物理归位或只做文档索引。

## 2026-04-28 Git 基线清洗：安全存档 + .gitignore 收口

- 涉及资产：全仓库（839 个文件）
- 当前路径：仓库根目录
- 目标分区：Git 版本控制
- 决策：`git add -A` 将全部未跟踪代码纳入 Git，创建安全快照 commit `1473a43`；补充 `var/*.log` 忽略规则
- 理由：检查发现 `apps/`、`infra/`、`packages/`、`tools/`、`e2e/`、`文档/` 等核心代码全部 untracked，一次误操作可能丢失全部工作
- 风险：generated seeds（15MB）已进入 Git，后续可能需评估是否改为生成后忽略
- 后续动作：
  1. 用户本机推送 GitHub：`git push -u origin main`
  2. 用户本机验证：`npm.cmd run check`
  3. 后续评估 `infra/cloudflare/d1/seeds/generated/` 的版本策略（见缺口 G006）

## 2026-04-28 demo 和归档资产默认不迁回正式主线

- 涉及资产：`apps/web/src/demo`、`apps/web/demo/mock-data`、`_archive_workspace`、`文档/归档`
- 当前路径：仓库现有 demo 与归档目录
- 目标分区：`07-参考实现与候选资产`、`99-隔离与待删除`
- 决策：只登记、只按需查找，不默认迁回正式主线。
- 理由：demo 和归档资产可能仍有设计或历史价值，但直接回灌会重新制造主线混乱。
- 风险：有价值的旧设计需要通过清单主动发现，不能靠全量浏览。
- 后续动作：为 demo 和归档资产建立“吸收 / 保留 / 归档 / 删除”分级清单。
