# 待整理清单

- 建立日期：2026-04-28
- 作用：记录还不能立即判断去留、需要从旧文件或归档中查找价值的资产。

## 使用规则

1. 这里记录“需要查”的对象，不代表要迁移。
2. 每一项查完后，要回填当前判断。
3. 如果判断为正式需要，则补入 `资产总表.md` 的正式资产项。
4. 如果判断为缺口，则补入 `缺口清单.md`。
5. 如果判断为废弃或隔离，则补入 `迁移决策记录.md`。

## 待整理项

| 编号 | 来源路径 | 疑问 | 可能用途 | 当前判断 | 下一步 | 优先级 |
|---|---|---|---|---|---|---|
| T001 | `_archive_workspace/design-and-drafts` | 是否还有可吸收到正式 UI 的设计稿、样式和页面结构 | UI 优化候选 | 待查 | 只按 UI 批次查找，不全量搬回 | 中 |
| T002 | `apps/web/src/demo` | 哪些 demo 页面还有正式化价值 | UI 样式、页面结构、交互候选 | 待查 | 对照正式页面逐页吸收成熟方案 | 高 |
| T003 | `apps/web/demo/mock-data` | 哪些静态数据仍被 bootstrap 或 demo 使用 | demo 种子、演示数据 | 待查 | 建使用者清单，避免误删 | 中 |
| T004 | `文档/归档` | 哪些阶段结论仍应上提到当前事实源 | 历史决策追溯 | 待查 | 只查阶段说明和当前相关主题 | 中 |
| T005 | `文档/过时` | 是否存在仍被当前文档引用的过时方案 | 风险排查 | 待查 | 检查链接和引用，避免旧口径混入 | 低 |
| T006 | `文档/学术答辩` | 哪些材料属于软件项目交付，哪些属于论文/答辩 | 学术交付资料 | 待查 | 与软件运行资产分开索引 | 低 |
| T007 | `参考文献` / `【参考文献】` | 两个参考文献目录是否重复 | 学术引用资料 | 待查 | 判断是否合并为一个事实源 | 低 |
| T008 | `infra/cloudflare/d1/seeds/generated` | 哪些 seed 必须版本化，哪些可生成后忽略 | 数据初始化资产 | 待查 | 标记生成命令、生成源和是否手改 | 高 |
| T009 | `infra/cloudflare/d1/rebuild` | rebuild SQL 是否仍有执行价值 | 数据修复/治理 | 待查 | 标注一次性脚本或长期工具 | 中 |
| T010 | `var/*.log` | 顶层日志是否应移动到 `var/logs` 或忽略 | 运行产物整理 | 待查 | 建清理规则，不进正式资产 | 中 |
| T011 | `.pydeps` / `.pydeps_runtime` | 是否只作为本地依赖缓存 | 环境依赖 | 待查 | 确认不纳入资产迁移 | 低 |
| T012 | Git 删除项 `prototype/*` | 旧 prototype 是否已完全迁入 `apps/web/demo` 或归档 | 旧前端原型 | 待查 | 对照当前新结构，确认 Git 基线 | 高 |
| T013 | Git 删除项 `scripts/*` | 旧脚本是否全部迁入 `tools/scripts` | 运行脚本 | 待查 | 对照 `package.json scripts` | 高 |
| T014 | Git 删除项 `cloudflare/d1/*` | 旧 D1 文件是否全部迁入 `infra/cloudflare/d1` | 数据库资产 | 待查 | 对照 migrations 和 seeds | 高 |
| T015 | 根级旧部署文件删除项 | `Procfile`、`railway.toml`、`runtime.txt` 是否已退出主线 | 旧部署资产 | 待查 | 确认 Railway 口径已过时 | 中 |
| T016 | `文档/进行中` | 哪些文件已经完成，应归档 | 当前执行文档瘦身 | 待查 | 控制当前工作面文件数量 | 高 |
| T017 | `文档/进行中/当前阶段总表.md` | 历史完成项是否过多 | 文档事实源瘦身 | 待瘦身 | 拆出阶段完成历史，只保留当前判断 | 高 |
| T018 | `apps/web/src/styles/*.css` | 页面 CSS 是否职责重叠 | UI 样式治理 | 待查 | 生成样式职责表 | 高 |
| T019 | `apps/web/src/components/ui` | UI 基础组件是否足够覆盖页面重复模式 | UI 组件治理 | 待查 | 建组件能力表 | 高 |
| T020 | `apps/web/src/components/business` | 业务组件是否有抽象边界 | 页面复用治理 | 待查 | 判断哪些应正式组件化 | 中 |

## 推荐整理顺序

1. 先查 T012 / T013 / T014，解决 Git 基线和旧结构对应关系。
2. 再查 T016 / T017，降低文档事实源噪音。
3. 再查 T002 / T018 / T019 / T020，推进 UI 继续优化。
4. 最后查 archive、学术材料、参考文献等低频资产。
